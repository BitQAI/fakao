# 客观题条目数据框架 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立「人审 md → 机洗 JSON → 清洗校验 → 听/看/抽查三视图」的客观题条目数据管线，落地 13 字段标准框架。

**Architecture:** md 是唯一人审真源（`03_条目库\<科目>.md`），`parse_md_to_json.py` 单向生成侧车 JSON（`fakao-entry/1.0`）；`validate_entries.py` 按硬错误 E01–E11 / 软警告 W01–W06 两级校验并落盘报告；`tts.py` 直读 JSON 的 `tts_text` 合成 mp3（保留旧 5 列 md 回退）；生产/督学两个 opencode subagent 分别负责「写库」与「派生三视图」。共享词表常量集中在 `fakao_schema.py`，md/JSON 读写集中在 `entry_io.py`，全部 TDD。

**Tech Stack:** Python 3.12（stdlib + PyYAML[已装] + pytest + edge-tts）、PowerShell 5.1、git（本地仓库无远端）、opencode subagents（`.opencode\agent\*.md`）。

## Global Constraints（每个任务隐含遵守）

- 编码：读文件一律 `utf-8-sig`（兼容带 BOM），写文件一律 `utf-8`；JSON 输出 `ensure_ascii=False`
- 所有脚本可从仓库任意目录执行：路径基于 `S.ROOT = Path(__file__).resolve().parents[1]` 定位仓库根
- 单元格内禁止 `|` 与换行；多值分隔符统一半角 `;`
- ID 枚举前缀含预留位 `XZ`(行政法)，但本轮只生产 7 科（民法 MF/刑法 XF/刑诉 XS/民诉 MS/商经知 SJ/理论法 LL/三国法 SG）
- 校验退出码：存在硬错误=1，否则=0；软警告不阻断
- 校验报告落 `99_过程文件\清洗校验\`（该目录被 .gitignore 整体排除，遵循 README「日志不入库」）
- 禁止新增 Global Constraints 之外的三方依赖；PyYAML 用于 front matter 解析（已装 6.0.3）
- **规则优先于示例**：spec 第三节示例行的结论句未带句号，与 E06 冲突——种子数据一律补句号（以第六节规则为准）
- git 提交信息沿用仓库首提交风格：`<type>: 中文描述`
- 已核对的仓库事实（工程师须知）：`scripts\`、`03_条目库\`、`音频\`、`状态\`、`99_过程文件\清洗校验\`、`.gitignore` 均不存在需新建；旧 `tts.py` 只存在于旧计划文档（`99_过程文件\项目规划文档\superpowers\plans\2026-08-18-案例锚点音频备考系统.md` Task 2）中，本计划 Task 7 按其参考实现新建；edge-tts 未安装

## File Structure（全景）

```
E:\法考\
├── .gitignore                          ← Task 1 新建
├── docs\superpowers\plans\…md          ← 本计划（Task 1 一并提交）
├── scripts\                            ← Task 2 起逐步新建
│   ├── fakao_schema.py                 词表/常量/正则/阈值（唯一权威定义处）
│   ├── entry_io.py                     md 解析 + JSON 模型 + sidecar 写盘
│   ├── parse_md_to_json.py             CLI：03_条目库\*.md → 同名 .json
│   ├── validate_entries.py             CLI：E01–E11 硬错误 + W01–W06 软警告 + 报告
│   └── tts.py                          CLI：JSON 直读合成 mp3 / 5 列 md 回退
├── tests\                              ← pytest 测试（Task 2 起）
│   ├── conftest.py                     sys.path 注入 + 样例文档 fixture
│   ├── test_fakao_schema.py
│   ├── test_entry_io.py
│   ├── test_parse_cli.py
│   ├── test_validate_hard.py
│   ├── test_validate_soft.py
│   └── test_tts.py
├── 03_条目库\                          ← Task 6 种子
│   ├── 刑法.md / 刑法.json
│   └── 商经知.md / 商经知.json
├── .opencode\agent\
│   ├── producer.md                     ← Task 8 生产 agent
│   └── scheduler.md                    ← Task 9 督学 agent
└── README.md                           ← Task 10 增补管线章节
```

---

### Task 1: Git 忽略规则与计划文档入库

**Files:**
- Create: `.gitignore`
- Commit: `docs\superpowers\plans\2026-08-25-客观题条目数据框架.md`（本文件）

**Interfaces:**
- Consumes: 无
- Produces: 干净的 git 追踪边界——后续任务只提交 `scripts\ tests\ 03_条目库\ .opencode\ docs\ .gitignore README.md`

- [ ] **Step 1: 写 `.gitignore`**

在仓库根创建 `.gitignore`，内容如下：

```gitignore
# ---- 过程文件（README 第二节：日志不入库、凭据勿提交）----
99_过程文件/

# ---- 运行产物 ----
__pycache__/
*.py[cod]
*.mp3
音频/
状态/

# ---- 编辑器 / 系统 ----
.~*
Thumbs.db
desktop.ini
```

- [ ] **Step 2: 验证忽略生效**

Run: `git status --short`
Expected: 不再出现 `?? 99_过程文件/`；仍可见 `?? .gitignore`、`?? README.md` 等

- [ ] **Step 3: 提交**

```powershell
git add .gitignore docs/superpowers/plans/; if ($?) { git commit -m "chore: 忽略过程文件与运行产物，入库实施计划" }
```

---

### Task 2: 受控词表模块 fakao_schema.py（含测试基建）

**Files:**
- Create: `scripts\fakao_schema.py`
- Create: `tests\conftest.py`（本任务先建 sys.path 注入部分）
- Test: `tests\test_fakao_schema.py`

**Interfaces:**
- Consumes: 无
- Produces（后续所有任务依赖的精确名字）:
  - 常量：`SCHEMA_NAME="fakao-entry/1.0"`、`SUBJECT_TO_PREFIX`（8 键 dict 含行政法→XZ）、`PREFIX_TO_SUBJECT`、`ID_PATTERN`、`PRIORITIES=("★","●","○")`、`TAGS`（6 元组）、`MAX_TAGS=3`、`SOURCE_TYPES`（9 元组）、`STATUSES=("draft","final")`、`HEADER`（13 元素 list）、`ANCHOR_MIN=16`、`ANCHOR_MAX=36`、`CONCLUSION_MAX=60`、`POINT_MAX=20`、`NOTE_MAX=80`、`TTS_TEXT_MAX=150`、`STATUTE_PATTERN`、`TTS_TEMPLATE`、`NO_STATUTE_PLACEHOLDER="无"`、`KNOWN_MATERIALS`（22 份材料名 frozenset）、`ROOT`（Path）
  - 函数：`force_utf8_stdio() -> None`

- [ ] **Step 1: 安装 pytest 并确认 PyYAML**

```powershell
python -m pip install pytest; python -c "import yaml; print(yaml.__version__)"
```

Expected: pytest 安装成功；打印 `6.0.3`（若 yaml 缺失则 `python -m pip install pyyaml`）

- [ ] **Step 2: 写失败测试 `tests\test_fakao_schema.py`**

```python
import fakao_schema as S


def test_subject_prefix_roundtrip():
    assert len(S.SUBJECT_TO_PREFIX) == 8  # 7 科 + 行政法预留
    for subj, pre in S.SUBJECT_TO_PREFIX.items():
        assert S.PREFIX_TO_SUBJECT[pre] == subj
    assert S.SUBJECT_TO_PREFIX["刑法"] == "XF"
    assert S.SUBJECT_TO_PREFIX["行政法"] == "XZ"


def test_id_pattern():
    for ok in ("MF-001", "XF-001", "XS-012", "MS-123", "SJ-001", "LL-099", "SG-100", "XZ-001"):
        assert S.ID_PATTERN.fullmatch(ok), ok
    for bad in ("XX-001", "XF-12", "xf-001", "XF-0001", "刑法-001", "XF-", "MFA-01"):
        assert not S.ID_PATTERN.fullmatch(bad), bad


def test_header_13_columns():
    assert len(S.HEADER) == 13
    assert S.HEADER[0] == "ID" and S.HEADER[-1] == "TTS文本"
    assert set(S.OPTIONAL_COLUMNS) == {"标签", "知识点备注", "TTS文本"}


def test_enums_sizes():
    assert len(S.SOURCE_TYPES) == 9
    assert tuple(sorted(S.TAGS)) == ("口诀", "数字", "对比", "新法", "计算", "观点展示")
    assert S.PRIORITIES == ("★", "●", "○")


def test_statute_pattern():
    for ok in ("刑法269条", "公司法第47条", "刑法第269条之一"):
        assert S.STATUTE_PATTERN.fullmatch(ok), ok
    for bad in ("269条", "刑法269", "刑法 269 条", "刑法第二百六十九条"):
        assert not S.STATUTE_PATTERN.fullmatch(bad), bad


def test_known_materials_22():
    assert len(S.KNOWN_MATERIALS) == 22
    assert "刑法-易错点.md" in S.KNOWN_MATERIALS
    assert "公司法-新旧对照表.md" in S.KNOWN_MATERIALS


def test_thresholds_and_template():
    assert (S.ANCHOR_MIN, S.ANCHOR_MAX, S.CONCLUSION_MAX,
            S.POINT_MAX, S.NOTE_MAX, S.TTS_TEXT_MAX) == (16, 36, 60, 20, 80, 150)
    assert S.TTS_TEMPLATE.format(subject="刑法", point="转化型抢劫",
                                 anchor="甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤",
                                 conclusion="成立抢劫罪。") == \
        "【刑法·转化型抢劫】甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤。成立抢劫罪。"


def test_root_points_at_repo():
    assert (S.ROOT / "README.md").exists()
```

- [ ] **Step 3: 建 `tests\conftest.py`（仅 path 注入）**

```python
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "scripts"))
```

- [ ] **Step 4: 跑测试确认失败**

Run: `python -m pytest tests -v`
Expected: FAIL / ERROR —— `ModuleNotFoundError: No module named 'fakao_schema'`

- [ ] **Step 5: 实现 `scripts\fakao_schema.py`**

```python
"""客观题条目数据框架 —— 受控词表与常量的唯一权威定义处。

依据规范：docs/superpowers/specs/2026-08-25-客观题条目数据框架-design.md
"""
import re
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]

SCHEMA_NAME = "fakao-entry/1.0"

# 科目 ↔ ID 前缀（行政法为预留位，材料投放后启用）
SUBJECT_TO_PREFIX = {
    "民法": "MF",
    "刑法": "XF",
    "刑诉": "XS",
    "民诉": "MS",
    "商经知": "SJ",
    "理论法": "LL",
    "三国法": "SG",
    "行政法": "XZ",
}
PREFIX_TO_SUBJECT = {v: k for k, v in SUBJECT_TO_PREFIX.items()}

ID_PATTERN = re.compile(r"^(MF|XF|XS|MS|SJ|LL|SG|XZ)-\d{3}$")

PRIORITIES = ("★", "●", "○")
TAGS = ("新法", "对比", "数字", "计算", "观点展示", "口诀")
MAX_TAGS = 3

SOURCE_TYPES = ("真题", "高频", "易错", "讲义", "对照", "论述", "新规", "研究", "案例")

STATUSES = ("draft", "final")

HEADER = ["ID", "科目", "子科目", "考点", "锚点句", "结论句", "优先级",
          "标签", "产出依据", "参考来源", "法条定位", "知识点备注", "TTS文本"]

OPTIONAL_COLUMNS = {"标签", "知识点备注", "TTS文本"}
NO_STATUTE_PLACEHOLDER = "无"

ANCHOR_MIN, ANCHOR_MAX = 16, 36
CONCLUSION_MAX = 60
POINT_MAX = 20
NOTE_MAX = 80
TTS_TEXT_MAX = 150

# 法条定位宽松正则（E09）：法律名(中文)+条文号，可带“第”“之一/之二/之三”
STATUTE_PATTERN = re.compile(r"^[\u4e00-\u9fa5]+第?\d+条(之[一二三])?$")

TTS_TEMPLATE = "【{subject}·{point}】{anchor}。{conclusion}"

# 已知 22 份源材料清单（W05 用；与 README 第一节一致）
KNOWN_MATERIALS = frozenset({
    # 备考研究（2）
    "法考备考研究·会话完整整理.md",
    "法考近5年命题趋势与1个月在职备考（客观题篇）.md",
    # 真题回忆版（1）
    "法考回忆版真题-近3年.md",
    # 三国法（2）
    "三国法-背诵版讲义.md", "三国法-高频考点.md",
    # 刑法（2）
    "刑法-高频考点.md", "刑法-易错点.md",
    # 刑诉（3）
    "刑诉-高频考点.md", "刑诉-易错点.md", "刑诉-新规汇编.md",
    # 商经知（4）
    "商经知-高频考点.md", "商经知-易错点.md", "商经知-背诵版讲义.md",
    "公司法-新旧对照表.md",
    # 民法（2）
    "民法-高频考点.md", "民法-易错点.md",
    # 民诉（2）
    "民诉-高频考点.md", "民诉-易错点.md",
    # 理论法（4）
    "理论法-高频考点.md", "理论法-易错点.md", "理论法-背诵版讲义.md",
    "法治思想-核心论述.md",
})


def force_utf8_stdio() -> None:
    """Windows 控制台默认 GBK，打印 ★●○ 等字符前统一切到 UTF-8。"""
    for stream in (sys.stdout, sys.stderr):
        if hasattr(stream, "reconfigure"):
            stream.reconfigure(encoding="utf-8", errors="replace")
```

- [ ] **Step 6: 跑测试确认通过**

Run: `python -m pytest tests -v`
Expected: `8 passed`

- [ ] **Step 7: 提交**

```powershell
git add scripts/fakao_schema.py tests/; if ($?) { git commit -m "feat: 受控词表模块 fakao_schema 与测试基建" }
```

---

### Task 3: 解析库 entry_io.py 与 parse_md_to_json.py

**Files:**
- Create: `scripts\entry_io.py`
- Create: `scripts\parse_md_to_json.py`
- Modify: `tests\conftest.py`（追加样例文档 fixtures）
- Test: `tests\test_entry_io.py`、`tests\test_parse_cli.py`

**Interfaces:**
- Consumes: Task 2 全部常量（`fakao_schema as S`）
- Produces:
  - `entry_io.read_text(path: Path) -> str`（utf-8-sig）
  - `entry_io.split_front_matter(text: str) -> tuple[dict | None, str, str | None]` → `(meta, body, error)`；error 以 `"E11"` 开头或为 None
  - `entry_io.split_row(line: str) -> list[str]`（去首尾各一个 `|` 后按 `|` 切分并 strip，保留尾部空单元格）
  - `entry_io.parse_rows(body: str) -> tuple[bool, list[tuple[int, list[str]]]]` → `(header_ok, rows)`，rows 元素为 `(行号, cells)`
  - `entry_io.parse_sources(raw: str) -> list[dict]` → `[{"type","ref","loc"}]`；不可解析片段记 `{"type": <原文>, "ref": "", "loc": ""}`
  - `entry_io.to_entry(cells: list[str]) -> dict`，键固定为：`id, subject, submodule, point, anchor, conclusion, priority, tags(list), rationale, sources(list), statutes(list), note(str|None), tts_override(str|None)`
  - `entry_io.build_tts_text(entry: dict) -> str`（override 非空则整段替换，否则套 `S.TTS_TEMPLATE`）
  - `entry_io.load_document(md_path: Path) -> dict`，键：`path, meta, fm_error, header_ok, rows, entries, sha256`
  - `entry_io.write_sidecar(doc: dict, out_path: Path | None = None) -> Path`，payload 键：`schema, source_md, status, generated_at, count, md_sha256, entries[]`（每条含派生 `tts_text`）
  - `parse_md_to_json.main(argv: list[str] | None = None) -> int`

- [ ] **Step 1: 扩充 `tests\conftest.py`（追加样例 fixtures）**

在既有 sys.path 注入代码之后追加：

```python
import pytest

SAMPLE_MD = """---
subject: 刑法
status: draft
batch: B1
updated: 2026-08-25
---

# 刑法条目库

| ID | 科目 | 子科目 | 考点 | 锚点句 | 结论句 | 优先级 | 标签 | 产出依据 | 参考来源 | 法条定位 | 知识点备注 | TTS文本 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| XF-001 | 刑法 | 分则-财产犯罪 | 转化型抢劫 | 甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤 | 成立抢劫罪：犯盗窃诈骗抢夺后为抗拒抓捕当场施暴（刑法269条）。 | ★ | [对比] | 历年高频，2025回忆版同考点 | 高频:刑法-高频考点.md#二.10;易错:刑法-易错点.md#40;真题:2025-卷一-T1 | 刑法269条 | 陷阱："当场"含追捕途中；14-16岁转化不成立抢劫 | |
"""


@pytest.fixture()
def sample_md_text():
    return SAMPLE_MD


@pytest.fixture()
def sample_md_file(tmp_path, sample_md_text):
    p = tmp_path / "刑法.md"
    p.write_text(sample_md_text, encoding="utf-8")
    return p
```

注意：样例结论句以句号结尾（E06 规则优先于 spec 示例）；锚点句实测 24 字（16–36 内）；最后一格 TTS文本 为空。

- [ ] **Step 2: 写失败测试 `tests\test_entry_io.py`**

```python
import hashlib
import json

import entry_io


# ---------- split_front_matter ----------

def test_split_front_matter_ok(sample_md_text):
    meta, body, err = entry_io.split_front_matter(sample_md_text)
    assert err is None
    assert meta["subject"] == "刑法" and meta["status"] == "draft"
    assert body.lstrip().startswith("# 刑法条目库")


def test_split_front_matter_missing():
    meta, body, err = entry_io.split_front_matter("# 没有头\n\n| A |\n")
    assert meta is None and err and err.startswith("E11")


def test_split_front_matter_unclosed():
    meta, body, err = entry_io.split_front_matter("---\nsubject: 刑法\n\n正文")
    assert meta is None and err and err.startswith("E11")


# ---------- split_row / parse_rows ----------

def test_split_row_keeps_empty_last_cell(sample_md_text):
    row = "| XF-001 | 刑法 | 分则-财产犯罪 | 转化型抢劫 | 甲盗窃后被打成轻伤了结此案 | 成立抢劫罪。 | ★ | [] | 高频 | 高频:刑法-高频考点.md#一 | 刑法269条 | | |"
    cells = entry_io.split_row(row)
    assert len(cells) == 13
    assert cells[-1] == "" and cells[-2] == ""


def test_split_row_strips_cells():
    cells = entry_io.split_row("| a | b |")
    assert cells == ["a", "b"]


def test_parse_rows_ok(sample_md_text):
    header_ok, rows = entry_io.parse_rows(entry_io.split_front_matter(sample_md_text)[1])
    assert header_ok is True
    assert len(rows) == 1
    lineno, cells = rows[0]
    assert lineno > 5 and len(cells) == 13 and cells[0] == "XF-001"


def test_parse_rows_bad_header(sample_md_text):
    body = entry_io.split_front_matter(sample_md_text)[1].replace("| 考点 |", "| 考察点 |")
    header_ok, rows = entry_io.parse_rows(body)
    assert header_ok is False and rows == []


def test_parse_rows_skips_separator_line(sample_md_text):
    _, rows = entry_io.parse_rows(entry_io.split_front_matter(sample_md_text)[1])
    assert all(cells[0] != "---" for _, cells in rows)


# ---------- parse_sources / to_entry ----------

def test_parse_sources_triples():
    got = entry_io.parse_sources("高频:刑法-高频考点.md#二.10;易错:刑法-易错点.md#40;真题:2025-卷一-T1")
    assert got == [
        {"type": "高频", "ref": "刑法-高频考点.md", "loc": "二.10"},
        {"type": "易错", "ref": "刑法-易错点.md", "loc": "40"},
        {"type": "真题", "ref": "2025-卷一", "loc": "T1"},
    ]


def test_parse_sources_unparseable_kept_as_marker():
    got = entry_io.parse_sources("随便写的")
    assert got == [{"type": "随便写的", "ref": "", "loc": ""}]


def test_to_entry_full(sample_md_text):
    _, cells = entry_io.parse_rows(entry_io.split_front_matter(sample_md_text)[1])[1][0]
    e = entry_io.to_entry(cells)
    assert e["id"] == "XF-001" and e["subject"] == "刑法"
    assert e["tags"] == ["对比"]
    assert e["statutes"] == ["刑法269条"]
    assert e["note"].startswith("陷阱：")
    assert e["tts_override"] is None
    assert len(e["sources"]) == 3


def test_to_entry_empty_optionals():
    cells = ["SG-001", "三国法", "国际私法", "侵权准据法", "甲乙两国公民在华驾车相撞起纠纷",
             "适用侵权行为地法。", "●", "", "高频考点", "高频:三国法-高频考点.md#四", "无", "", ""]
    e = entry_io.to_entry(cells)
    assert e["tags"] == [] and e["note"] is None and e["tts_override"] is None
    assert e["statutes"] == ["无"]


# ---------- build_tts_text ----------

def test_build_tts_text_template(sample_md_text):
    _, cells = entry_io.parse_rows(entry_io.split_front_matter(sample_md_text)[1])[1][0]
    text = entry_io.build_tts_text(entry_io.to_entry(cells))
    assert text == ("【刑法·转化型抢劫】甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤。"
                    "成立抢劫罪：犯盗窃诈骗抢夺后为抗拒抓捕当场施暴（刑法269条）。")


def test_build_tts_text_override_wins():
    e = {"subject": "刑法", "point": "考点", "anchor": "锚", "conclusion": "结论。",
         "tts_override": "自定义播报内容"}
    assert entry_io.build_tts_text(e) == "自定义播报内容"


# ---------- load_document / write_sidecar ----------

def test_load_document_fields(sample_md_file):
    doc = entry_io.load_document(sample_md_file)
    assert doc["fm_error"] is None and doc["header_ok"] is True
    assert doc["meta"]["subject"] == "刑法"
    assert len(doc["entries"]) == 1
    raw = entry_io.read_text(sample_md_file)
    assert doc["sha256"] == hashlib.sha256(raw.encode("utf-8")).hexdigest()


def test_read_text_tolerates_bom(tmp_path, sample_md_text):
    p = tmp_path / "带BOM.md"
    p.write_text("\ufeff" + sample_md_text, encoding="utf-8")
    doc = entry_io.load_document(p)
    assert doc["header_ok"] is True and len(doc["entries"]) == 1


def test_write_sidecar_payload(sample_md_file, tmp_path):
    doc = entry_io.load_document(sample_md_file)
    out = tmp_path / "刑法.json"
    got = entry_io.write_sidecar(doc, out)
    assert got == out
    payload = json.loads(out.read_text(encoding="utf-8"))
    assert payload["schema"] == "fakao-entry/1.0"
    assert payload["status"] == "draft"
    assert payload["count"] == 1
    assert "md_sha256" in payload and "generated_at" in payload
    assert payload["source_md"].endswith("刑法.md")
    first = payload["entries"][0]
    assert first["id"] == "XF-001" and first["priority"] == "★"
    assert first["tts_override"] is None
    assert first["tts_text"].startswith("【刑法·转化型抢劫】")
    assert first["sources"][0] == {"type": "高频", "ref": "刑法-高频考点.md", "loc": "二.10"}
```

- [ ] **Step 3: 跑测试确认失败**

Run: `python -m pytest tests/test_entry_io.py -v`
Expected: FAIL / ERROR —— `No module named 'entry_io'`

- [ ] **Step 4: 实现 `scripts\entry_io.py`**

```python
"""md 条目库解析与侧车 JSON 写盘（单向：md 是唯一真源）。"""
import hashlib
import json
import re
from datetime import datetime
from pathlib import Path

import yaml

import fakao_schema as S

_SEP_CELL = re.compile(r"^:?-{3,}:?$")
_SOURCE_PART = re.compile(r"^(?P<type>[^:#]+):(?P<ref>[^:#]+)#(?P<loc>.+)$")


def read_text(path: Path) -> str:
    return path.read_text(encoding="utf-8-sig")


def split_front_matter(text: str):
    lines = text.splitlines()
    if not lines or lines[0].strip() != "---":
        return None, text, "E11: 缺少 front matter（首行须为 ---）"
    end = None
    for i in range(1, len(lines)):
        if lines[i].strip() == "---":
            end = i
            break
    if end is None:
        return None, text, "E11: front matter 未闭合（缺第二个 ---）"
    meta = yaml.safe_load("\n".join(lines[1:end]))
    if not isinstance(meta, dict):
        return None, "\n".join(lines[end + 1:]), "E11: front matter 不是键值映射"
    return meta, "\n".join(lines[end + 1:]), None


def split_row(line: str) -> list:
    s = line.strip()
    if s.startswith("|"):
        s = s[1:]
    if s.endswith("|"):
        s = s[:-1]
    return [c.strip() for c in s.split("|")]


def parse_rows(body: str):
    header_ok = False
    rows = []
    seen_header = False
    for lineno, line in enumerate(body.splitlines(), 1):
        t = line.strip()
        if not t.startswith("|"):
            continue
        cells = split_row(t)
        if cells and all(_SEP_CELL.match(c) for c in cells):
            continue
        if not seen_header:
            seen_header = True
            if cells != S.HEADER:
                return False, []
            header_ok = True
            continue
        rows.append((lineno, cells))
    return header_ok, rows


def parse_sources(raw: str) -> list:
    out = []
    for part in (p.strip() for p in raw.split(";")):
        if not part:
            continue
        m = _SOURCE_PART.match(part)
        if m:
            out.append({"type": m.group("type"), "ref": m.group("ref"), "loc": m.group("loc")})
        else:
            out.append({"type": part, "ref": "", "loc": ""})
    return out


def to_entry(cells: list) -> dict:
    (rid, subject, submodule, point, anchor, conclusion,
     priority, tags_raw, rationale, sources_raw, statutes_raw, note, tts_override) = cells
    tags = [t for t in (x.strip() for x in tags_raw.strip("[]").split("/")) if t]
    statutes = [s for s in (x.strip() for x in statutes_raw.split(";")) if s]
    return {
        "id": rid,
        "subject": subject,
        "submodule": submodule,
        "point": point,
        "anchor": anchor,
        "conclusion": conclusion,
        "priority": priority,
        "tags": tags,
        "rationale": rationale,
        "sources": parse_sources(sources_raw),
        "statutes": statutes,
        "note": note or None,
        "tts_override": tts_override or None,
    }


def build_tts_text(entry: dict) -> str:
    if entry.get("tts_override"):
        return entry["tts_override"]
    return S.TTS_TEMPLATE.format(subject=entry["subject"], point=entry["point"],
                                 anchor=entry["anchor"], conclusion=entry["conclusion"])


def load_document(md_path: Path) -> dict:
    text = read_text(Path(md_path))
    meta, body, fm_error = split_front_matter(text)
    header_ok, rows = parse_rows(body)
    entries = [to_entry(cells) for _, cells in rows if len(cells) == len(S.HEADER)]
    return {
        "path": Path(md_path),
        "meta": meta,
        "fm_error": fm_error,
        "header_ok": header_ok,
        "rows": rows,
        "entries": entries,
        "sha256": hashlib.sha256(text.encode("utf-8")).hexdigest(),
    }


def write_sidecar(doc: dict, out_path: Path | None = None) -> Path:
    md_path = Path(doc["path"])
    out = Path(out_path) if out_path else md_path.with_suffix(".json")
    payload = {
        "schema": S.SCHEMA_NAME,
        "source_md": md_path.as_posix(),
        "status": (doc["meta"] or {}).get("status"),
        "generated_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
        "count": len(doc["entries"]),
        "md_sha256": doc["sha256"],
        "entries": [],
    }
    for e in doc["entries"]:
        item = dict(e)
        item["tts_text"] = build_tts_text(e)
        payload["entries"].append(item)
    out.write_text(json.dumps(payload, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
    return out
```

- [ ] **Step 5: 跑测试确认通过**

Run: `python -m pytest tests/test_entry_io.py -v`
Expected: `17 passed`

- [ ] **Step 6: 写 CLI 失败测试 `tests\test_parse_cli.py`**

```python
import json

from parse_md_to_json import main


def test_cli_ok_writes_sibling_json(sample_md_file):
    rc = main([str(sample_md_file)])
    assert rc == 0
    j = sample_md_file.with_suffix(".json")
    assert j.exists()
    payload = json.loads(j.read_text(encoding="utf-8"))
    assert payload["count"] == 1 and payload["entries"][0]["id"] == "XF-001"


def test_cli_bad_header_aborts(tmp_path, sample_md_text, capsys):
    bad = tmp_path / "坏.md"
    bad.write_text(sample_md_text.replace("| 考点 |", "| 考察点 |"), encoding="utf-8")
    rc = main([str(bad)])
    assert rc == 1
    assert "E01" in capsys.readouterr().out
    assert not bad.with_suffix(".json").exists()


def test_cli_bad_source_aborts(tmp_path, sample_md_text, capsys):
    bad = tmp_path / "坏.md"
    bad.write_text(
        sample_md_text.replace(
            "高频:刑法-高频考点.md#二.10;易错:刑法-易错点.md#40;真题:2025-卷一-T1",
            "随手编造的来源"),
        encoding="utf-8")
    rc = main([str(bad)])
    assert rc == 1
    assert "不可解析" in capsys.readouterr().out
```

- [ ] **Step 7: 跑测试确认失败**

Run: `python -m pytest tests/test_parse_cli.py -v`
Expected: FAIL —— `No module named 'parse_md_to_json'`

- [ ] **Step 8: 实现 `scripts\parse_md_to_json.py`**

```python
"""03_条目库\\*.md → 同名 .json（机洗真源，禁止手改）。

用法：
    python scripts\\parse_md_to_json.py                # 全部 03_条目库\\*.md
    python scripts\\parse_md_to_json.py 03_条目库\\刑法.md
结构非法时拒绝写出并提示先跑 validate_entries.py。
"""
import argparse
from pathlib import Path

import entry_io
import fakao_schema as S


def collect_problems(doc: dict) -> list:
    problems = []
    if doc["fm_error"]:
        problems.append(doc["fm_error"])
    if not doc["header_ok"]:
        problems.append("E01: 表头与 13 列规范不一致")
    for lineno, cells in doc["rows"]:
        if len(cells) != len(S.HEADER):
            problems.append(f"E01: 第 {lineno} 行 {len(cells)} 列≠{len(S.HEADER)}")
    for e in doc["entries"]:
        for src in e["sources"]:
            if not src["ref"]:
                problems.append(f"E08: 来源不可解析三元组：{src['type']}（{e['id']}）")
    return problems


def main(argv=None) -> int:
    S.force_utf8_stdio()
    ap = argparse.ArgumentParser(description="条目库 md → 侧车 JSON")
    ap.add_argument("paths", nargs="*", help="待转换 md；缺省 = 03_条目库\\*.md")
    args = ap.parse_args(argv)

    paths = [Path(p) for p in args.paths] or sorted((S.ROOT / "03_条目库").glob("*.md"))
    if not paths:
        print("没有找到待转换的 md 文件")
        return 0

    docs, all_problems = [], []
    for p in paths:
        doc = entry_io.load_document(p)
        docs.append(doc)
        all_problems.extend(f"{p.name}: {msg}" for msg in collect_problems(doc))

    if all_problems:
        print("转换中止，先修复以下问题（可运行 validate_entries.py 辅助定位）：")
        for msg in all_problems:
            print(f"  {msg}")
        return 1

    for doc in docs:
        out = entry_io.write_sidecar(doc)
        rel = out.relative_to(S.ROOT).as_posix()
        print(f"OK: {doc['path'].name} -> {rel} ({len(doc['entries'])} 条)")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 9: 全量跑测试确认通过**

Run: `python -m pytest tests -v`
Expected: `28 passed`（8 schema + 17 entry_io + 3 parse_cli），0 failed

- [ ] **Step 10: 提交**

```powershell
git add scripts/entry_io.py scripts/parse_md_to_json.py tests/; if ($?) { git commit -m "feat: md→JSON 解析库与 parse_md_to_json 命令" }
```

---

### Task 4: 校验器 validate_entries.py —— 硬错误 E01–E11

**Files:**
- Create: `scripts\validate_entries.py`
- Test: `tests\test_validate_hard.py`

**Interfaces:**
- Consumes: Task 2 常量；Task 3 的 `entry_io.load_document / to_entry / build_tts_text`
- Produces:
  - `validate_entries.validate_files(paths: list[Path], material_names: set | None = None, known_materials: set | None = None) -> tuple[list, list]` → `(hard_errors, soft_warnings)`；元素形如 `{"code": "E05", "file": "...", "line": 12, "msg": "..."}`
  - `validate_entries.levenshtein(a: str, b: str) -> int`（Task 5 使用，本任务一并实现）
  - `validate_entries.build_report(files: list[Path], hard: list, soft: list) -> dict`
  - `validate_entries.write_report(payload: dict, dest_dir: Path | None = None) -> Path`（缺省落 `99_过程文件\清洗校验\报告-YYYYMMDD-HHMM.json`）
  - `validate_entries.main(argv=None) -> int`

**规则映射（严格按 spec 第六节，含两处已定解释）：**
- E04 覆盖必填空单元格：科目/子科目/考点/锚点句/结论句/优先级/产出依据/参考来源/法条定位（列索引 1,2,3,4,5,6,8,9,10）
- 法条定位为空白 → 只报 E04（必填空），不再重复报 E09；E09 仅对非空且≠`无`的值做正则校验
- E08 文件存在性检查仅针对以 `.md` 结尾的出处 ref（真题 `YYYY-卷一`、案例库名等非文件 ref 跳过）
- W05 与 E08 共用 ref 存在性：ref 在磁盘树中但不在 `KNOWN_MATERIALS` 22 份清单内时才报 W05（树外文件由 E08 兜底）

- [ ] **Step 1: 写失败测试 `tests\test_validate_hard.py`**

```python
from validate_entries import levenshtein, validate_files

GOOD_ANCHOR = "甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤"      # 24 字
GOOD_CONCL = "成立抢劫罪：犯盗窃诈骗抢夺后为抗拒抓捕当场施暴（刑法269条）。"  # 32 字

GOOD_ROW = ("| XF-001 | 刑法 | 分则-财产犯罪 | 转化型抢劫 | " + GOOD_ANCHOR +
            " | " + GOOD_CONCL +
            " | ★ | [对比] | 历年高频 | 高频:刑法-高频考点.md#二.10;真题:2025-卷一-T1 "
            "| 刑法269条 | 陷阱：当场含追捕途中 | |")

HEADER_LINE = ("| ID | 科目 | 子科目 | 考点 | 锚点句 | 结论句 | 优先级 | 标签 | 产出依据 "
               "| 参考来源 | 法条定位 | 知识点备注 | TTS文本 |")
SEP_LINE = "|---|---|---|---|---|---|---|---|---|---|---|---|---|"

FM = "---\nsubject: 刑法\nstatus: draft\nbatch: B1\nupdated: 2026-08-25\n---\n\n# 刑法条目库\n\n"

MATERIALS = {"刑法-高频考点.md", "刑法-易错点.md", "公司法-新旧对照表.md", "民法-高频考点.md"}


def make_doc(tmp_path, *rows, name="刑法.md", fm=FM, header=HEADER_LINE):
    p = tmp_path / name
    p.write_text(fm + header + "\n" + SEP_LINE + "\n" + "".join(r + "\n" for r in rows),
                 encoding="utf-8")
    return p


def codes(items):
    return [i["code"] for i in items]


def hard_only(tmp_path, *rows, **kw):
    p = make_doc(tmp_path, *rows, **kw)
    hard, soft = validate_files([p], material_names=MATERIALS,
                                known_materials={"刑法-高频考点.md"})
    return hard, soft


# ---------- 基线 ----------

def test_clean_row_passes(tmp_path):
    hard, soft = hard_only(tmp_path, GOOD_ROW)
    assert hard == [] and soft == []


# ---------- E01 ----------

def test_e01_bad_header(tmp_path):
    bad_header = HEADER_LINE.replace("| 考点 |", "| 考察点 |")
    hard, _ = hard_only(tmp_path, GOOD_ROW, header=bad_header)
    assert codes(hard) == ["E01"]


def test_e01_wrong_col_count(tmp_path):
    short = GOOD_ROW.rsplit("|", 1)[0]        # 掉最后一个空格 -> 12 列
    hard, _ = hard_only(tmp_path, GOOD_ROW, short)
    assert codes(hard) == ["E01"]
    assert hard[0]["line"] > 0


# ---------- E02 ----------

def test_e02_bad_format(tmp_path):
    hard, _ = hard_only(tmp_path, GOOD_ROW.replace("XF-001", "XX-001"))
    assert "E02" in codes(hard)


def test_e02_duplicate_id(tmp_path):
    hard, _ = hard_only(tmp_path, GOOD_ROW, GOOD_ROW.replace(GOOD_ANCHOR, "乙深夜翻墙入院窃走手机两部的现场"))
    dup = [e for e in hard if e["code"] == "E02" and "重复" in e["msg"]]
    assert len(dup) == 1


def test_e02_prefix_subject_mismatch(tmp_path):
    hard, _ = hard_only(tmp_path, GOOD_ROW.replace("XF-001", "MF-001"))
    assert "E02" in codes(hard)


# ---------- E03 ----------

def test_e03_subject_not_in_enum(tmp_path):
    hard, _ = hard_only(tmp_path, GOOD_ROW.replace("| 刑法 | 分则", "| 行政法学 | 分则"))
    assert "E03" in codes(hard)


# ---------- E04 ----------

def test_e04_required_blank(tmp_path):
    hard, _ = hard_only(tmp_path,
                        GOOD_ROW.replace("| 分则-财产犯罪 |", "| | "))
    assert any(e["code"] == "E04" and "子科目" in e["msg"] for e in hard)


def test_e04_sources_blank(tmp_path):
    blank_src = ("| XF-002 | 刑法 | 分则-财产犯罪 | 抢劫 | " + GOOD_ANCHOR +
                 " | " + GOOD_CONCL + " | ★ | [] | 高频 |  | 刑法269条 | | |")
    hard, _ = hard_only(tmp_path, blank_src)
    assert any(e["code"] == "E04" and "参考来源" in e["msg"] for e in hard)


# ---------- E05 ----------

def test_e05_too_short(tmp_path):
    short_anchor = "甲盗窃后被人赃俱获"          # 9 字
    hard, _ = hard_only(tmp_path, GOOD_ROW.replace(GOOD_ANCHOR, short_anchor))
    assert any(e["code"] == "E05" and "长度" in e["msg"] for e in hard)


def test_e05_too_long(tmp_path):
    long_anchor = "甲" * 37
    hard, _ = hard_only(tmp_path, GOOD_ROW.replace(GOOD_ANCHOR, long_anchor))
    assert any(e["code"] == "E05" for e in hard)


def test_e05_ends_with_period(tmp_path):
    hard, _ = hard_only(tmp_path, GOOD_ROW.replace(GOOD_ANCHOR, GOOD_ANCHOR + "。"))
    assert any(e["code"] == "E05" and "句号" in e["msg"] for e in hard)


def test_e05_boundary_lengths_ok(tmp_path):
    r16 = GOOD_ROW.replace(GOOD_ANCHOR, "甲" * 16)                       # 恰好 16 字
    r36 = GOOD_ROW.replace(GOOD_ANCHOR, "甲" * 36).replace("XF-001", "XF-002")  # 恰好 36 字
    hard, _ = hard_only(tmp_path, r16, r36)
    assert [e for e in hard if e["code"] == "E05"] == []


# ---------- E06 ----------

def test_e06_no_period(tmp_path):
    hard, _ = hard_only(tmp_path,
                        GOOD_ROW.replace(GOOD_CONCL, GOOD_CONCL[:-1]))   # 去掉句号
    assert any(e["code"] == "E06" and "句号" in e["msg"] for e in hard)


def test_e06_too_long(tmp_path):
    hard, _ = hard_only(tmp_path,
                        GOOD_ROW.replace(GOOD_CONCL, "甲" * 60 + "。"))
    assert any(e["code"] == "E06" and "60" in e["msg"] for e in hard)


# ---------- E07 ----------

def test_e07_bad_priority(tmp_path):
    hard, _ = hard_only(tmp_path, GOOD_ROW.replace("| ★ |", "| ☆ |"))
    assert any(e["code"] == "E07" and "优先级" in e["msg"] for e in hard)


def test_e07_unknown_tag(tmp_path):
    hard, _ = hard_only(tmp_path, GOOD_ROW.replace("[对比]", "[超纲]"))
    assert any(e["code"] == "E07" and "标签非法" in e["msg"] for e in hard)


def test_e07_too_many_tags(tmp_path):
    hard, _ = hard_only(tmp_path, GOOD_ROW.replace("[对比]", "[新法/对比/数字/口诀]"))
    assert any(e["code"] == "E07" and "超过3个" in e["msg"] for e in hard)


# ---------- E08 ----------

def test_e08_unparseable_source(tmp_path):
    bad_row = GOOD_ROW.replace("高频:刑法-高频考点.md#二.10;真题:2025-卷一-T1", "随手乱写")
    hard, _ = hard_only(tmp_path, bad_row)
    assert any(e["code"] == "E08" and "不可解析" in e["msg"] for e in hard)


def test_e08_unknown_type(tmp_path):
    bad_row = GOOD_ROW.replace("高频:刑法-高频考点.md#二.10", "野鸡:刑法-高频考点.md#二.10")
    hard, _ = hard_only(tmp_path, bad_row)
    assert any(e["code"] == "E08" and "类型非法" in e["msg"] for e in hard)


def test_e08_file_not_in_tree(tmp_path):
    bad_row = GOOD_ROW.replace("高频:刑法-高频考点.md#二.10", "高频:幽灵-高频考点.md#一")
    hard, _ = hard_only(tmp_path, bad_row)
    assert any(e["code"] == "E08" and "不在 01_客观题" in e["msg"] for e in hard)


# ---------- E09 ----------

def test_e09_statute_pattern_fail(tmp_path):
    hard, _ = hard_only(tmp_path, GOOD_ROW.replace("| 刑法269条 |", "| 刑法269 |"))
    assert any(e["code"] == "E09" for e in hard)


def test_e09_none_placeholder_ok(tmp_path):
    no_statute = GOOD_ROW.replace("高频:刑法-高频考点.md#二.10;真题:2025-卷一-T1",
                                  "讲义:刑法-高频考点.md#总则").replace("| 刑法269条 |", "| 无 |")
    hard, _ = hard_only(tmp_path, no_statute)
    assert [e for e in hard if e["code"] == "E09"] == []


# ---------- E10 ----------

def test_e10_override_over_150(tmp_path):
    long_override = "长" * 151
    hard, _ = hard_only(tmp_path,
                        GOOD_ROW.replace("陷阱：当场含追捕途中 | |",
                                         f"陷阱：当场含追捕途中 | {long_override} |"))
    assert any(e["code"] == "E10" and "150" in e["msg"] for e in hard)


# ---------- E11 ----------

def test_e11_no_front_matter(tmp_path):
    p = make_doc(tmp_path, GOOD_ROW, fm="# 直接正文\n\n")
    hard, _ = validate_files([p], material_names=MATERIALS,
                             known_materials={"刑法-高频考点.md"})
    assert codes(hard).count("E11") >= 1


def test_e11_bad_status(tmp_path):
    fm = FM.replace("status: draft", "status: published")
    p = make_doc(tmp_path, GOOD_ROW, fm=fm)
    hard, _ = validate_files([p], material_names=MATERIALS,
                             known_materials={"刑法-高频考点.md"})
    assert any(e["code"] == "E11" and "status" in e["msg"] for e in hard)


# ---------- levenshtein（Task 5 W04 依赖）----------

def test_levenshtein_basic():
    assert levenshtein("", "") == 0
    assert levenshtein("abc", "abc") == 0
    assert levenshtein("kitten", "sitting") == 3
    assert levenshtein("abc", "") == 3
```

注意 `test_e08_unparseable_source` 里第一个断言块是冗余探底（保留无害）；核心断言是 `hard3` 部分。

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_validate_hard.py -v`
Expected: FAIL —— `No module named 'validate_entries'`

- [ ] **Step 3: 实现 `scripts\validate_entries.py`**

```python
"""清洗校验：硬错误 E01–E11（阻断定稿）+ 软警告 W01–W06（人工复核）。

用法：
    python scripts\\validate_entries.py                        # 全部 03_条目库\\*.md
    python scripts\\validate_entries.py 03_条目库\\刑法.md
    python scripts\\validate_entries.py --no-report            # 不写报告文件
退出码：有硬错误=1，否则=0。
"""
import argparse
import json
from datetime import datetime
from pathlib import Path

import entry_io
import fakao_schema as S

_REQUIRED_IDX_NAME = [(1, "科目"), (2, "子科目"), (3, "考点"), (4, "锚点句"),
                      (5, "结论句"), (6, "优先级"), (8, "产出依据"),
                      (9, "参考来源"), (10, "法条定位")]


def default_material_names() -> set:
    base = S.ROOT / "01_客观题"
    if not base.exists():
        return set()
    return {p.name for p in base.rglob("*") if p.is_file()}


def levenshtein(a: str, b: str) -> int:
    if a == b:
        return 0
    if not a:
        return len(b)
    if not b:
        return len(a)
    prev = list(range(len(b) + 1))
    for i, ca in enumerate(a, 1):
        cur = [i]
        for j, cb in enumerate(b, 1):
            cur.append(min(prev[j] + 1, cur[j - 1] + 1, prev[j - 1] + (ca != cb)))
        prev = cur
    return prev[-1]


def validate_files(paths, material_names=None, known_materials=None):
    material_names = default_material_names() if material_names is None else material_names
    known_materials = set(S.KNOWN_MATERIALS) if known_materials is None else known_materials
    hard, soft = [], []

    def err(code, file, line, msg):
        hard.append({"code": code, "file": str(file), "line": line, "msg": msg})

    def warn(code, file, line, msg):
        soft.append({"code": code, "file": str(file), "line": line, "msg": msg})

    seen_ids = {}
    for path in paths:
        path = Path(path)
        rel = path.as_posix()
        doc = entry_io.load_document(path)

        meta = doc["meta"]
        if doc["fm_error"]:
            err("E11", rel, 1, doc["fm_error"])
        else:
            status = meta.get("status")
            if status not in S.STATUSES:
                err("E11", rel, 1, f"status 非法：{status!r}（允许 draft/final）")
            if not meta.get("subject"):
                err("E11", rel, 1, "front matter 缺 subject")

        if not doc["header_ok"]:
            err("E01", rel, None, "表头与 13 列规范不一致")
            continue

        file_points = []
        anchors_by_submodule = {}
        for lineno, cells in doc["rows"]:
            if len(cells) != len(S.HEADER):
                err("E01", rel, lineno, f"{len(cells)} 列≠{len(S.HEADER)}")
                continue
            (rid, subject, submodule, point, anchor, conclusion,
             priority, _tags_raw, _rationale, _sources_raw,
             _statutes_raw, note, _tts_override) = cells

            if not S.ID_PATTERN.fullmatch(rid):
                err("E02", rel, lineno, f"ID 格式错：{rid}")
            else:
                if rid in seen_ids:
                    err("E02", rel, lineno, f"ID 全局重复：{rid}（首次见于 {seen_ids[rid]}）")
                else:
                    seen_ids[rid] = f"{rel}:{lineno}"
                prefix = rid.split("-")[0]
                if subject in S.SUBJECT_TO_PREFIX and S.SUBJECT_TO_PREFIX[subject] != prefix:
                    err("E02", rel, lineno, f"前缀 {prefix} 与科目 {subject} 不匹配")

            if subject not in S.SUBJECT_TO_PREFIX:
                err("E03", rel, lineno, f"科目不在受控枚举：{subject}")

            for idx, name in _REQUIRED_IDX_NAME:
                if not cells[idx].strip():
                    err("E04", rel, lineno, f"必填字段为空:{name}")

            entry = entry_io.to_entry(cells)

            e5 = []
            if not (S.ANCHOR_MIN <= len(anchor) <= S.ANCHOR_MAX):
                e5.append(f"长度{len(anchor)}不在{S.ANCHOR_MIN}–{S.ANCHOR_MAX}")
            if anchor.endswith("。"):
                e5.append("以句号结尾")
            if e5:
                err("E05", rel, lineno, "；".join(e5))

            e6 = []
            if len(conclusion) > S.CONCLUSION_MAX:
                e6.append(f"长度{len(conclusion)}>60")
            if not conclusion.endswith("。"):
                e6.append("不以句号结尾")
            if e6:
                err("E06", rel, lineno, "；".join(e6))

            if priority not in S.PRIORITIES:
                err("E07", rel, lineno, f"优先级非法：{priority}")
            if len(entry["tags"]) > S.MAX_TAGS:
                err("E07", rel, lineno, f"标签超过{S.MAX_TAGS}个：{entry['tags']}")
            for tag in entry["tags"]:
                if tag not in S.TAGS:
                    err("E07", rel, lineno, f"标签非法：{tag}")

            for src in entry["sources"]:
                if not src["ref"]:
                    err("E08", rel, lineno, f"来源不可解析三元组：{src['type']}")
                    continue
                if src["type"] not in S.SOURCE_TYPES:
                    err("E08", rel, lineno, f"来源类型非法：{src['type']}（{src['ref']}）")
                if src["ref"].endswith(".md"):
                    if src["ref"] not in material_names:
                        err("E08", rel, lineno, f"出处文件不在 01_客观题 树：{src['ref']}")
                    elif src["ref"] not in known_materials:
                        warn("W05", rel, lineno, f"出处不在已知22份材料清单：{src['ref']}")

            statutes = entry["statutes"]
            if statutes != [S.NO_STATUTE_PLACEHOLDER]:
                for st in statutes:
                    if not S.STATUTE_PATTERN.fullmatch(st):
                        err("E09", rel, lineno, f"法条定位不匹配宽松正则：{st}")

            tts_text = entry_io.build_tts_text(entry)
            if len(tts_text) > S.TTS_TEXT_MAX:
                err("E10", rel, lineno, f"TTS 文本{len(tts_text)}字>{S.TTS_TEXT_MAX}")

            if len(point) > S.POINT_MAX:
                warn("W01", rel, lineno, f"考点{len(point)}字>{S.POINT_MAX}")
            if note and len(note) > S.NOTE_MAX:
                warn("W02", rel, lineno, f"备注{len(note)}字>{S.NOTE_MAX}")
            if statutes == [S.NO_STATUTE_PLACEHOLDER] and "条" not in conclusion:
                warn("W06", rel, lineno, "理论条目请确认：法条定位=无 且 结论句不含『条』")

            file_points.append(point)
            anchors_by_submodule.setdefault(submodule, []).append(anchor)

        counts = {}
        for pt in file_points:
            counts[pt] = counts.get(pt, 0) + 1
        for pt, n in counts.items():
            if n > 1:
                warn("W03", rel, None, f"同文件考点完全重复×{n}：{pt}")

        for submodule, anchors in anchors_by_submodule.items():
            for i in range(len(anchors)):
                for j in range(i + 1, len(anchors)):
                    dist = levenshtein(anchors[i], anchors[j])
                    if 0 < dist <= 6:
                        warn("W04", rel, None,
                             f"子科目[{submodule}]锚点相似(编辑距离{dist})：{anchors[i][:12]}…")
    return hard, soft


def build_report(files, hard, soft) -> dict:
    return {
        "generated_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
        "files": [Path(p).as_posix() for p in files],
        "hard_error_count": len(hard),
        "soft_warning_count": len(soft),
        "hard_errors": hard,
        "soft_warnings": soft,
    }


def write_report(payload: dict, dest_dir: Path | None = None) -> Path:
    d = Path(dest_dir) if dest_dir else S.ROOT / "99_过程文件" / "清洗校验"
    d.mkdir(parents=True, exist_ok=True)
    out = d / f"报告-{datetime.now().strftime('%Y%m%d-%H%M')}.json"
    out.write_text(json.dumps(payload, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
    return out


def main(argv=None) -> int:
    S.force_utf8_stdio()
    ap = argparse.ArgumentParser(description="条目库清洗校验（硬错误阻断定稿）")
    ap.add_argument("paths", nargs="*", help="待校验 md；缺省 = 03_条目库\\*.md")
    ap.add_argument("--no-report", action="store_true", help="不写报告 JSON")
    args = ap.parse_args(argv)

    paths = [Path(p) for p in args.paths] or sorted((S.ROOT / "03_条目库").glob("*.md"))
    if not paths:
        print("没有找到待校验的 md 文件")
        return 0

    hard, soft = validate_files(paths)
    for e in hard:
        print(f"[{e['code']}] {e['file']}:{e['line']} {e['msg']}")
    for w in soft:
        print(f"[{w['code']}] {w['file']}:{w['line']} {w['msg']}")
    verdict = "阻断定稿" if hard else "通过"
    print(f"\n汇总：硬错误 {len(hard)}，软警告 {len(soft)} -> {verdict}")

    if not args.no_report:
        report = write_report(build_report(paths, hard, soft))
        print(f"报告：{report.relative_to(S.ROOT).as_posix()}")
    return 1 if hard else 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_validate_hard.py -v`
Expected: `27 passed`（clean/E01×2/E02×3/E03/E04×2/E05×4/E06×2/E07×3/E08×3/E09×2/E10/E11×2/levenshtein），0 failed

- [ ] **Step 5: 提交**

```powershell
git add scripts/validate_entries.py tests/test_validate_hard.py; if ($?) { git commit -m "feat: 清洗校验器硬错误 E01–E11" }
```

---

### Task 5: 校验器软警告 W01–W06、CLI 报告与退出码

**Files:**
- Modify: `scripts\validate_entries.py`（W 规则已在 Task 4 实现主体中——本任务补齐测试与 CLI 级验证）
- Test: `tests\test_validate_soft.py`（含 W 规则 + CLI 退出码/报告两组测试）

**Interfaces:**
- Consumes: Task 4 的 `validate_files / build_report / write_report / main`
- Produces: 软警告行为与报告/退出码的回归保障（无新增公共接口）

说明：Task 4 的实现里 W01–W06 主体逻辑已就位（避免同一函数改两遍）；本任务用独立测试文件锁定行为，并补 CLI 层（报告落盘、退出码）测试。这是对「reviewer 可独立否决 W 规则」边界的尊重——测试与实现在同一提交序列中先后落地。

- [ ] **Step 1: 写失败测试 `tests\test_validate_soft.py`**

脚手架（`GOOD_ROW / make_doc 等`）与 Task 4 同构，在本文件内独立定义，保持测试文件彼此可独立运行：

```python
import json
import re

from validate_entries import validate_files, build_report, write_report, main

GOOD_ANCHOR = "甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤"
GOOD_CONCL = "成立抢劫罪：犯盗窃诈骗抢夺后为抗拒抓捕当场施暴（刑法269条）。"
GOOD_ROW = ("| XF-001 | 刑法 | 分则-财产犯罪 | 转化型抢劫 | " + GOOD_ANCHOR +
            " | " + GOOD_CONCL +
            " | ★ | [对比] | 历年高频 | 高频:刑法-高频考点.md#二.10 "
            "| 刑法269条 | 陷阱：当场含追捕途中 | |")
HEADER_LINE = ("| ID | 科目 | 子科目 | 考点 | 锚点句 | 结论句 | 优先级 | 标签 | 产出依据 "
               "| 参考来源 | 法条定位 | 知识点备注 | TTS文本 |")
SEP_LINE = "|---|---|---|---|---|---|---|---|---|---|---|---|---|"
FM = "---\nsubject: 刑法\nstatus: draft\nbatch: B1\nupdated: 2026-08-25\n---\n\n# t\n\n"
MATERIALS = {"刑法-高频考点.md", "刑法-易错点.md", "公司法-新旧对照表.md", "民法-高频考点.md"}
KNOWN = {"刑法-高频考点.md"}


def run(tmp_path, *rows, name="刑法.md", fm=FM):
    p = tmp_path / name
    p.write_text(fm + HEADER_LINE + "\n" + SEP_LINE + "\n" +
                 "".join(r + "\n" for r in rows), encoding="utf-8")
    return validate_files([p], material_names=MATERIALS, known_materials=KNOWN)


# ---------- W01 / W02 ----------

def test_w01_point_over_20(tmp_path):
    hard, soft = run(tmp_path, GOOD_ROW.replace("| 转化型抢劫 |", f"| {'转' * 21} |"))
    assert hard == [] and any(w["code"] == "W01" for w in soft)


def test_w02_note_over_80(tmp_path):
    hard, soft = run(tmp_path, GOOD_ROW.replace("陷阱：当场含追捕途中", "陷" * 81))
    assert hard == [] and any(w["code"] == "W02" for w in soft)


# ---------- W03 ----------

def test_w03_duplicate_point(tmp_path):
    row2 = GOOD_ROW.replace("XF-001", "XF-002") \
                   .replace(GOOD_ANCHOR, "乙深夜潜入便利店挟持店员劫走营业款后逃离")
    hard, soft = run(tmp_path, GOOD_ROW, row2)
    assert hard == [] and any(w["code"] == "W03" for w in soft)


# ---------- W04 ----------

def test_w04_similar_anchor_same_submodule(tmp_path):
    near = GOOD_ANCHOR.replace("打成轻伤", "打成轻微伤")     # 编辑距离 1
    row2 = GOOD_ROW.replace("XF-001", "XF-002").replace(GOOD_ANCHOR, near)
    hard, soft = run(tmp_path, GOOD_ROW, row2)
    assert hard == [] and any(w["code"] == "W04" for w in soft)


def test_w04_different_submodule_no_warn(tmp_path):
    near = GOOD_ANCHOR.replace("打成轻伤", "打成轻微伤")
    row2 = GOOD_ROW.replace("XF-001", "XF-002") \
                   .replace("分则-财产犯罪", "分则-人身犯罪") \
                   .replace(GOOD_ANCHOR, near)
    hard, soft = run(tmp_path, GOOD_ROW, row2)
    assert hard == [] and not any(w["code"] == "W04" for w in soft)


# ---------- W05 ----------

def test_w05_ref_outside_known_list_but_in_tree(tmp_path):
    row = GOOD_ROW.replace("高频:刑法-高频考点.md#二.10", "高频:民法-高频考点.md#三")
    hard, soft = run(tmp_path, row)      # 民法 ref 在 MATERIALS(树) 中、不在 KNOWN(22清单) 中
    assert hard == [] and any(w["code"] == "W05" for w in soft)


# ---------- W06 ----------

def test_w06_theory_entry_hint(tmp_path):
    theory = ("| LL-001 | 理论法 | 法理学 | 法律解释方法 | 文义解释与体系解释发生冲突时如何取舍 | "
              "优先文义解释，辅以体系解释校验。 | ○ | [] | 高频考点 "
              "| 讲义:理论法-背诵版讲义.md#法理学 | 无 | | |")
    hard, soft = run(tmp_path, theory, name="理论法.md",
                     fm=FM.replace("subject: 刑法", "subject: 理论法"))
    assert hard == [] and any(w["code"] == "W06" for w in soft)


# ---------- CLI 报告与退出码 ----------

def test_cli_exit_codes(tmp_path, capsys):
    ok = tmp_path / "刑法.md"
    ok.write_text(FM + HEADER_LINE + "\n" + SEP_LINE + "\n" + GOOD_ROW + "\n",
                  encoding="utf-8")
    assert main(["--no-report", str(ok)]) == 0
    bad = tmp_path / "坏.md"
    bad.write_text((FM + HEADER_LINE + "\n" + SEP_LINE + "\n" +
                    GOOD_ROW.replace("| ★ |", "| ☆ |")), encoding="utf-8")
    assert main(["--no-report", str(bad)]) == 1
    assert "E07" in capsys.readouterr().out


def test_write_report_filename_and_payload(tmp_path):
    out = write_report(build_report([], [], []), dest_dir=tmp_path)
    assert re.fullmatch(r"报告-\d{8}-\d{4}\.json", out.name)
    data = json.loads(out.read_text(encoding="utf-8"))
    assert data["hard_error_count"] == 0 and data["soft_warning_count"] == 0
    assert set(data) == {"generated_at", "files", "hard_error_count",
                         "soft_warning_count", "hard_errors", "soft_warnings"}
```

说明：`test_cli_exit_codes` 用 `--no-report` 避免测试污染真实 `99_过程文件\清洗校验\`；报告落盘由下一个用例以 `dest_dir=tmp_path` 验证。

- [ ] **Step 2: 跑测试确认通过**

Run: `python -m pytest tests/test_validate_soft.py -v`
Expected: `9 passed`（W01/W02/W03/W04×2/W05/W06 + CLI×2；W 规则主体在 Task 4 已实现故应直接绿——若某条意外红，按该条修 `validate_entries.py` 至绿）

- [ ] **Step 3: 全量回归**

Run: `python -m pytest tests -v`
Expected: `73 passed`（8+17+3+27+9+9），0 failed

- [ ] **Step 4: 提交**

```powershell
git add tests/test_validate_soft.py scripts/validate_entries.py; if ($?) { git commit -m "test: 软警告 W01–W06 与校验 CLI 报告/退出码回归" }
```

---

### Task 6: 条目库种子样例（端到端冒烟）

**Files:**
- Create: `03_条目库\刑法.md`
- Create: `03_条目库\商经知.md`
- Generate: `03_条目库\刑法.json`、`03_条目库\商经知.json`（脚本产物，随种子一并提交）

**Interfaces:**
- Consumes: Task 3 `parse_md_to_json.py`、Task 4/5 `validate_entries.py`
- Produces: 两份符合规范的种子条目（spec 第三节两条示例行，按规则补句号），作为生产 agent 的风格基准与管线端到端验证物

- [ ] **Step 1: 写 `03_条目库\刑法.md`**

```markdown
---
subject: 刑法
status: draft
batch: B1
updated: 2026-08-25
---

# 刑法条目库

| ID | 科目 | 子科目 | 考点 | 锚点句 | 结论句 | 优先级 | 标签 | 产出依据 | 参考来源 | 法条定位 | 知识点备注 | TTS文本 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| XF-001 | 刑法 | 分则-财产犯罪 | 转化型抢劫 | 甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤 | 成立抢劫罪：犯盗窃诈骗抢夺后为抗拒抓捕当场施暴（刑法269条）。 | ★ | [对比] | 历年高频，2025回忆版同考点 | 高频:刑法-高频考点.md#二.10;易错:刑法-易错点.md#40;真题:2025-卷一-T1 | 刑法269条 | 陷阱："当场"含追捕途中；14-16岁转化不成立抢劫 | |
```

- [ ] **Step 2: 写 `03_条目库\商经知.md`**

```markdown
---
subject: 商经知
status: draft
batch: B1
updated: 2026-08-25
---

# 商经知条目库

| ID | 科目 | 子科目 | 考点 | 锚点句 | 结论句 | 优先级 | 标签 | 产出依据 | 参考来源 | 法条定位 | 知识点备注 | TTS文本 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| SJ-001 | 商经知 | 公司法-资本制度 | 五年实缴制 | 2024年8月设立的公司，章程约定2032年才缴足出资 | 有限公司认缴出资须自成立起5年内缴足，存量公司最迟2032年6月底前调整（公司法47条）。 | ★ | [新法/数字] | 新法必考：2023修订核心变化 | 对照:公司法-新旧对照表.md#47;高频:商经知-高频考点.md#考点2 | 公司法47条 | 存量公司"3+5"过渡：2027年6月30日前修改章程 | |
```

- [ ] **Step 3: 校验种子**

Run: `python scripts\validate_entries.py`
Expected: 退出码 0；控制台 `汇总：硬错误 0，软警告 0 -> 通过`；生成 `99_过程文件\清洗校验\报告-*.json`

- [ ] **Step 4: 生成侧车 JSON**

Run: `python scripts\parse_md_to_json.py`
Expected: 退出码 0；输出两行 `OK: … -> 03_条目库/<科目>.json (1 条)`

- [ ] **Step 5: 抽查 JSON 内容**

Run: `python -c "import json;d=json.load(open(r'03_条目库\刑法.json',encoding='utf-8'));print(d['schema'],d['count'],d['entries'][0]['tts_text'])"`
Expected: `fakao-entry/1.0 1 【刑法·转化型抢劫】甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤。成立抢劫罪：…（刑法269条）。`

- [ ] **Step 6: 提交**

```powershell
git add 03_条目库/; if ($?) { git commit -m "feat: 条目库种子样例（刑法/商经知各1条，spec第三节示例）" }
```

---

### Task 7: TTS 合成命令 tts.py（JSON 直读 + 5 列回退 + dry-run）

**Files:**
- Create: `scripts\tts.py`
- Test: `tests\test_tts.py`

**Interfaces:**
- Consumes: `entry_io.split_row`（回退模式复用切行逻辑）；侧车 JSON 结构（Task 3）
- Produces:
  - `VOICE = "zh-CN-XiaoxiaoNeural"`
  - `load_json_entries(path: Path) -> list`（校验 schema 名不符即 `SystemExit`）
  - `parse_legacy(md_path: Path) -> list`（旧 5 列格式：`ID|科目|考点|锚点句|结论句`，跳过表头）
  - `entry_text(e: dict) -> str`（有 `tts_text` 用之；否则模板拼装）
  - `synth(text: str, out: Path, dry_run: bool = False) -> None`
  - `main(argv=None) -> int`；CLI 参数 `--input --out [--concat] [--dry-run]`；`.json` 后缀走直读，其余按 5 列 md 回退

- [ ] **Step 1: 安装 edge-tts**

```powershell
python -m pip install edge-tts
```

Expected: 安装成功。（离线环境可跳过——后续测试只用 dry-run，不触发网络）

- [ ] **Step 2: 写失败测试 `tests\test_tts.py`**

```python
import json

import entry_io
import tts
from tts import VOICE, load_json_entries, parse_legacy, synth, entry_text, main


LEGACY_MD = (
    "# 听包清单\n\n"
    "| ID | 科目 | 考点 | 锚点句 | 结论句 |\n"
    "|---|---|---|---|---|\n"
    "| XF-001 | 刑法 | 转化型抢劫 | 甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤 "
    "| 成立抢劫罪：犯盗窃诈骗抢夺后为抗拒抓捕当场施暴（刑法269条）。 |\n"
    "| SJ-001 | 商经知 | 五年实缴制 | 2024年8月设立的公司，章程约定2032年才缴足出资 "
    "| 有限公司认缴出资须自成立起5年内缴足。 |\n")


def make_sidecar(tmp_path, sample_md_file):
    doc = entry_io.load_document(sample_md_file)
    return entry_io.write_sidecar(doc, tmp_path / "刑法.json")


def test_load_json_entries(tmp_path, sample_md_file):
    j = make_sidecar(tmp_path, sample_md_file)
    entries = load_json_entries(j)
    assert len(entries) == 1 and entries[0]["id"] == "XF-001"
    assert entries[0]["tts_text"].startswith("【刑法·转化型抢劫】")


def test_load_json_entries_rejects_bad_schema(tmp_path):
    j = tmp_path / "x.json"
    j.write_text(json.dumps({"schema": "other/9.9"}), encoding="utf-8")
    try:
        load_json_entries(j)
        assert False, "应当 SystemExit"
    except SystemExit:
        pass


def test_parse_legacy(tmp_path):
    p = tmp_path / "听包清单.md"
    p.write_text(LEGACY_MD, encoding="utf-8")
    entries = parse_legacy(p)
    assert [e["id"] for e in entries] == ["XF-001", "SJ-001"]
    assert entries[0]["subject"] == "刑法" and entries[0]["point"] == "转化型抢劫"


def test_entry_text_prefers_tts_text():
    assert entry_text({"tts_text": "覆盖", "subject": "刑法"}) == "覆盖"
    assert entry_text({"subject": "刑法", "point": "考点", "anchor": "场景",
                       "conclusion": "结论。"}) == "【刑法·考点】场景。结论。"


def test_synth_dry_run_creates_nothing(tmp_path, capsys):
    out = tmp_path / "a.mp3"
    synth("文本", out, dry_run=True)
    assert not out.exists()
    assert "dry-run" in capsys.readouterr().out


def test_synth_builds_edge_tts_command(monkeypatch, tmp_path):
    recorded = []
    monkeypatch.setattr(tts.subprocess, "run", lambda cmd, check: recorded.append(cmd))
    out = tmp_path / "a.mp3"
    synth("你好", out, dry_run=False)
    assert recorded == [["edge-tts", "--voice", VOICE, "--text", "你好",
                         "--write-media", str(out)]]


def test_main_dry_run_json_mode(tmp_path, sample_md_file, capsys):
    j = make_sidecar(tmp_path, sample_md_file)
    out_dir = tmp_path / "听包"
    rc = main(["--input", str(j), "--out", str(out_dir), "--dry-run"])
    assert rc == 0
    assert not list(out_dir.rglob("*.mp3"))
    assert "OK: 1 条" in capsys.readouterr().out


def test_main_legacy_mode_dry_run(tmp_path, capsys):
    p = tmp_path / "听包清单.md"
    p.write_text(LEGACY_MD, encoding="utf-8")
    rc = main(["--input", str(p), "--out", str(tmp_path / "听包"), "--concat", "--dry-run"])
    assert rc == 0
    assert "OK: 2 条" in capsys.readouterr().out


def test_main_empty_input_fails(tmp_path, capsys):
    p = tmp_path / "空.md"
    p.write_text("# 没有表格\n", encoding="utf-8")
    rc = main(["--input", str(p), "--out", str(tmp_path / "o")])
    assert rc == 1
    assert "未解析到任何条目" in capsys.readouterr().err
```

- [ ] **Step 3: 跑测试确认失败**

Run: `python -m pytest tests/test_tts.py -v`
Expected: FAIL —— `No module named 'tts'`

- [ ] **Step 4: 实现 `scripts\tts.py`**

```python
"""条目 → mp3 听包合成。

默认直读侧车 JSON（tts_text 由 parse_md_to_json 预生成）；
输入为 5 列 md（听包清单）时自动回退到旧版解析。
edge-tts 不可用/离线时可加 --dry-run 只打印待合成文本。

用法：
    python scripts\\tts.py --input 03_条目库\\刑法.json --out 音频\\试听 --concat
    python scripts\\tts.py --input 音频\\2026-08-25\\听包清单.md --out 音频\\2026-08-25
"""
import argparse
import json
import subprocess
import sys
from pathlib import Path

import entry_io
import fakao_schema as S

VOICE = "zh-CN-XiaoxiaoNeural"


def load_json_entries(path: Path) -> list:
    payload = json.loads(path.read_text(encoding="utf-8-sig"))
    if payload.get("schema") != S.SCHEMA_NAME:
        raise SystemExit(f"ERROR: schema 不符：{payload.get('schema')!r}（期望 {S.SCHEMA_NAME}）")
    return payload.get("entries", [])


def parse_legacy(md_path: Path) -> list:
    entries = []
    for line in entry_io.read_text(md_path).splitlines():
        line = line.strip()
        if not line.startswith("|"):
            continue
        cells = entry_io.split_row(line)
        if len(cells) < 5 or cells[0] == "ID":
            continue
        entries.append({"id": cells[0], "subject": cells[1], "point": cells[2],
                        "anchor": cells[3], "conclusion": cells[4]})
    return entries


def entry_text(e: dict) -> str:
    if e.get("tts_text"):
        return e["tts_text"]
    return f"【{e['subject']}·{e['point']}】{e['anchor']}。{e['conclusion']}"


def synth(text: str, out: Path, dry_run: bool = False) -> None:
    if dry_run:
        print(f"[dry-run] {out} <= {text[:50]}{'…' if len(text) > 50 else ''}")
        return
    subprocess.run(["edge-tts", "--voice", VOICE, "--text", text,
                    "--write-media", str(out)], check=True)


def main(argv=None) -> int:
    S.force_utf8_stdio()
    ap = argparse.ArgumentParser(description="法考条目 → mp3 听包")
    ap.add_argument("--input", required=True, help="侧车 .json 或旧版 5 列 .md")
    ap.add_argument("--out", required=True, help="输出目录")
    ap.add_argument("--concat", action="store_true", help="额外合成 整包.mp3")
    ap.add_argument("--dry-run", action="store_true", help="只打印文本，不调用 edge-tts")
    args = ap.parse_args(argv)

    src = Path(args.input)
    if src.suffix.lower() == ".json":
        entries = load_json_entries(src)
    else:
        entries = parse_legacy(src)
    if not entries:
        print("ERROR: 未解析到任何条目（检查输入格式）", file=sys.stderr)
        return 1

    out_dir = Path(args.out)
    entries_dir = out_dir / "entries"
    entries_dir.mkdir(parents=True, exist_ok=True)

    concat_text = ""
    for e in entries:
        text = entry_text(e)
        synth(text, entries_dir / f"{e['id']}.mp3", args.dry_run)
        concat_text += text + "。"
    if args.concat:
        synth(concat_text, out_dir / "整包.mp3", args.dry_run)

    suffix = "（dry-run 未实际合成）" if args.dry_run else ""
    print(f"OK: {len(entries)} 条 -> {out_dir}{suffix}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 5: 跑测试确认通过**

Run: `python -m pytest tests/test_tts.py -v`
Expected: `9 passed`

- [ ] **Step 6: 真实合成冒烟（可选，需联网）**

```powershell
python scripts\tts.py --input "03_条目库\刑法.json" --out "音频\试听" --concat
```

Expected: `OK: 1 条 -> 音频\试听`；`音频\试听\entries\XF-001.mp3` 与 `音频\试听\整包.mp3` 生成且 >1KB。若 edge-tts 网络报错，改用 `--dry-run` 验证文本输出即可，不阻塞任务收尾。

Run（验证产物，联网成功时）: `$f = Get-ChildItem "音频\试听" -Recurse -Filter *.mp3; $f.Count; ($f | Where-Object { $_.Length -lt 1KB }).Count`
Expected: `2`、`0`

- [ ] **Step 7: 提交**

```powershell
git add scripts/tts.py tests/test_tts.py; if ($?) { git commit -m "feat: TTS 合成命令（JSON 直读+5列回退+dry-run）" }
```

---

### Task 8: 生产 agent（.opencode\agent\producer.md）

**Files:**
- Create: `.opencode\agent\producer.md`

**Interfaces:**
- Consumes: spec 第一/二节词表；`scripts\validate_entries.py`（Task 4/5）
- Produces: 可被 opencode 以 subagent 方式调用的生产 agent；调用约定：给它的提示里指明目标科目与源材料范围，它产出/更新 `03_条目库\<科目>.md` 并自跑到校验零硬错误

- [ ] **Step 1: 写 `.opencode\agent\producer.md`**

````markdown
---
description: 法考条目生产：读取 01_客观题 源材料，按 13 字段规范产出/更新 03_条目库\<科目>.md，直至校验零硬错误
mode: subagent
tools:
  - read
  - glob
  - grep
  - write
  - edit
  - bash
---

你是法考客观题条目生产 agent。你的唯一交付物是 `03_条目库\<科目>.md` 中符合规范的条目行。

## 工作流

1. 通读指定科目的全部源材料（`01_客观题\科目资料\<科目>\` 下所有 md；用户另有指定时从其指定）。
2. 提炼候选考点清单（宁缺毋滥：只收「值得背诵记忆」的考点，排除纯理解性叙述）。
3. 若 `03_条目库\<科目>.md` 已存在，先读取，接续最大序号新增；确有错误才修改旧行。
4. 按「13 字段规范」「受控词表」「单元格语法」逐条写成表格行。
5. 运行 `python scripts\validate_entries.py 03_条目库\<科目>.md`。
6. 修复全部 E 硬错误；W 软警告逐条判断：能优化则优化（缩短考点/备注、消除近似锚点），确属误报可保留并在汇报中列出理由。
7. 汇报：新增 N 条 / 修改 M 条 / 剩余 W 警告清单及理由。不要自行把 status 改为 final（定稿由人工决定）。

## 13 字段规范（顺序固定）

| # | 字段 | 要求 |
|---|---|---|
| 1 | ID | `<前缀>-三位序号`，全局唯一；前缀必须与科目一致 |
| 2 | 科目 | 受控枚举 7 科之一 |
| 3 | 子科目 | 二级模块，「编-节」式命名，从下方词表取 |
| 4 | 考点 | 大纲语言，≤20 字（超 20 字仅警告） |
| 5 | 锚点句 | 案例/场景，16–36 字（含标点），具体生动，不以句号结尾 |
| 6 | 结论句 | 一句话独立成立，≤60 字，以句号结尾，口语顺带关键法条 |
| 7 | 优先级 | ★高频 / ●易错 / ○普通 单选 |
| 8 | 标签 | ⊆{新法,对比,数字,计算,观点展示,口诀}，0–3 个，写作 `[新法/对比]` |
| 9 | 产出依据 | 为何值得记，一句话 |
| 10 | 参考来源 | ≥1 条，每条 `类型:出处#定位`，多条用 `;` 分隔 |
| 11 | 法条定位 | `法律名+条文号`，多条 `;` 分隔；纯理论填 `无` |
| 12 | 知识点备注 | 易错陷阱/关联，≤80 字，可空 |
| 13 | TTS文本 | 通常留空（自动生成）；确需覆盖 ≤150 字 |

## 受控词表

科目↔前缀：民法 MF / 刑法 XF / 刑诉 XS / 民诉 MS / 商经知 SJ / 理论法 LL / 三国法 SG（行政法 XZ 为预留，本轮不用）。

子科目范围：
- 民法：总则 / 物权 / 合同 / 人格权 / 婚姻家庭 / 继承 / 侵权
- 刑法：总则-犯罪构成 / 总则-形态 / 总则-共犯 / 总则-罪数 / 总则-刑罚 / 分则-人身犯罪 / 分则-财产犯罪 / 分则-贪贿 / 分则-公共安全 / 分则-市场秩序 / 分则-社会管理
- 刑诉：原则 / 管辖 / 回避 / 辩护 / 证据 / 强制措施 / 侦查 / 起诉 / 审判 / 复核 / 再审 / 执行 / 特别程序
- 民诉：基本制度 / 管辖 / 当事人 / 第三人 / 证据 / 调解 / 保全 / 程序 / 简易小额 / 二审 / 再审 / 非讼 / 执行 / 涉外
- 商经知：公司法 / 合伙 / 破产 / 票据 / 保险 / 证券 / 反垄断 / 经济法小法 / 知产 / 劳动社保环资
- 理论法：法治思想 / 法理学 / 宪法 / 法律史 / 司法制度与职业道德
- 三国法：国际公法 / 国际私法 / 国际经济法

来源类型 9 种（出处必须是真实存在的文件名，定位用 `#章节/编号`）：真题(`YYYY-卷一`)、高频(`<科目>-高频考点.md`)、易错(`<科目>-易错点.md`)、讲义(`<科目>-背诵版讲义.md`)、对照(`公司法-新旧对照表.md`)、论述(`法治思想-核心论述.md`)、新规(`刑诉-新规汇编.md`)、研究(备考研究文件)、案例(`人民法院案例库`)。

## 单元格语法（红线）

- 单元格内禁止出现 `|` 和换行；多值分隔符只用半角 `;`
- 可空单元格仅限：标签、知识点备注、TTS文本；法条定位没有条文时填 `无`

## 文件骨架（新文件照抄此结构，只换科目名与行）

```markdown
---
subject: 刑法
status: draft
batch: B1
updated: YYYY-MM-DD
---

# 刑法条目库

| ID | 科目 | 子科目 | 考点 | 锚点句 | 结论句 | 优先级 | 标签 | 产出依据 | 参考来源 | 法条定位 | 知识点备注 | TTS文本 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| XF-001 | 刑法 | 分则-财产犯罪 | 转化型抢劫 | 甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤 | 成立抢劫罪：犯盗窃诈骗抢夺后为抗拒抓捕当场施暴（刑法269条）。 | ★ | [对比] | 历年高频，2025回忆版同考点 | 高频:刑法-高频考点.md#二.10;易错:刑法-易错点.md#40;真题:2025-卷一-T1 | 刑法269条 | 陷阱："当场"含追捕途中；14-16岁转化不成立抢劫 | |
```

## 质量红线

- 参考来源必须真实可回溯：出处文件名来自上表 9 种类型对应的真实文件，`#定位` 要能在该文件中找到；严禁编造出处
- 锚点句写「案发瞬间」，不写法言法语堆砌；结论句面向耳朵（口语化），法条定位面向机器（规范引用），二者不强求一致
- 一行一条，禁止在单元格内展开论述
````

- [ ] **Step 2: 结构自检**

Run: `Select-String -Path ".opencode\agent\producer.md" -Pattern "^mode:|^tools:" `
Expected: 命中 `mode: subagent` 与 tools 列表；再 `Select-String -Path ".opencode\agent\producer.md" -Pattern "13 字段规范"` 命中

- [ ] **Step 3: 提交**

```powershell
git add .opencode/agent/producer.md; if ($?) { git commit -m "feat: 生产 agent（13字段/7科词表/自跑校验）" }
```

---

### Task 9: 督学 agent（.opencode\agent\scheduler.md）

**Files:**
- Create: `.opencode\agent\scheduler.md`

**Interfaces:**
- Consumes: `03_条目库\*.json`（status=final 才可用）；`scripts\tts.py`（Task 7）；`状态\` 下历史抽查记录
- Produces: 每日三件套 `音频\<日期>\听包清单.md`（+mp3）、`看包.md`、`状态\<日期>抽查.md`

- [ ] **Step 1: 写 `.opencode\agent\scheduler.md`**

````markdown
---
description: 督学调度：读取定稿条目 JSON，按日程选条生成每日听包/看包/抽查三件套并合成音频
mode: subagent
tools:
  - read
  - glob
  - grep
  - write
  - edit
  - bash
---

你是法考督学调度 agent。每天一次，从定稿条目库中选条并产出三件套。

## 输入

- `03_条目库\*.json`：只消费顶层 `"status": "final"` 的文件（draft 一律不碰）
- `状态\` 目录：历史抽查记录（答错的条目 ID 即 ⚠错条）
- 用户给出的当日参数：日期、当日新学条数 N（缺省 10）

## 选条与排序

1. 收集候选池 = 所有 final 条目
2. 排序权重：⚠错条 ＞ 滚动复习（上次学习日期最早者优先）＞ 新条（未学过）
3. 同组内按优先级 ★ ＞ ● ＞ ○，同级保持 ID 序稳定
4. 取前 N 条为今日清单

## 产出 1：`音频\<日期>\听包清单.md`

旧 5 列格式（兼容 `tts.py` 回退模式），表头固定：

```markdown
| ID | 科目 | 考点 | 锚点句 | 结论句 |
|---|---|---|---|---|
| XF-001 | 刑法 | 转化型抢劫 | 甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤 | 成立抢劫罪：犯盗窃诈骗抢夺后为抗拒抓捕当场施暴（刑法269条）。 |
```

随后执行合成：

```powershell
python scripts\tts.py --input "音频\<日期>\听包清单.md" --out "音频\<日期>"
```

（edge-tts 离线时报错则加 `--dry-run` 并在汇报中注明「音频待补」。）

## 产出 2：`音频\<日期>\看包.md`

⚠错条置顶；每条一块，格式：

```markdown
## XF-001 转化型抢劫 〔对比〕〔★〕

- 场景：甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤
- 结论：成立抢劫罪：犯盗窃诈骗抢夺后为抗拒抓捕当场施暴（刑法269条）
- 依据：历年高频，2025回忆版同考点
- 法条：刑法269条
- 陷阱："当场"含追捕途中；14-16岁转化不成立抢劫
- 来源：高频:刑法-高频考点.md#二.10；真题:2025-卷一-T1
```

标签逐枚渲染为 `〔xx〕` 徽标（紧跟标题）；备注有值才渲染「陷阱：」行；TTS 相关字段一律不出现在看包。

## 产出 3：`状态\<日期>抽查.md`

对今日清单每条出一道挖空题：

```markdown
## 1. XF-001 转化型抢劫

场景：甲盗窃后被失主当场扭住，为挣脱反抗将失主打成轻伤

问：结论是什么？（关键处已挖空）

> 成立____罪：犯____、____、____后为抗拒抓捕当场施暴（刑法____条）

A. 抢劫罪 / 盗窃 诈骗 抢夺 / 269
B. 抢夺罪 / 盗窃 诈骗 敲诈 / 267
C. 侵占罪 / 遗忘物 代保管物 / 270

---
```

挖空规则：罪名、多值要件关键词、条文号替换为 `____`；干扰项 B/C 从同批其他条目的「陷阱」表述或其他考点的结论句改写，保持长度相近；文末附答案区（倒排）。

## 收尾

1. 把今日清单条目 ID 与「新学/滚动/错条重做」标记追加写入 `状态\<日期>记录.md`
2. 汇报：三件套路径、选中条数、⚠错条数、音频是否成功
````

- [ ] **Step 2: 结构自检**

Run: `Select-String -Path ".opencode\agent\scheduler.md" -Pattern "status.*final|听包清单|抽查"`
Expected: 三类关键词均有命中

- [ ] **Step 3: 提交**

```powershell
git add .opencode/agent/scheduler.md; if ($?) { git commit -m "feat: 督学 agent（三视图派生+排序+合成调度）" }
```

---

### Task 10: README 管线章节与全量回归

**Files:**
- Modify: `README.md`（文末追加一节）
- Test: 全量 pytest + 两条 CLI 冒烟

**Interfaces:**
- Consumes: 前 9 个任务全部产物
- Produces: 文档闭环 + 绿灯基线

- [ ] **Step 1: 在 `README.md` 文末追加**

```markdown
---

## 六、客观题条目库管线（03_条目库）

> 设计规范：`docs\superpowers\specs\2026-08-25-客观题条目数据框架-design.md`｜实施计划：`docs\superpowers\plans\2026-08-25-客观题条目数据框架.md`

| 内容 | 说明 |
|---|---|
| 数据 | `03_条目库\<科目>.md` 人审真源（唯一编辑入口）＋同名 `.json` 机洗真源（禁止手改，单向生成） |
| 生成 | `python scripts\parse_md_to_json.py`（md → json，含 front matter/表头/tts_text/hash） |
| 校验 | `python scripts\validate_entries.py`（硬错误 E01–E11 阻断定稿；软警告 W01–W06 人工复核；报告落 `99_过程文件\清洗校验\`） |
| 音频 | `python scripts\tts.py --input <条目.json|听包清单.md> --out <目录>`（JSON 直读 tts_text；5 列 md 回退；`--dry-run` 离线演练） |
| Agent | `.opencode\agent\producer.md` 生产条目；`.opencode\agent\scheduler.md` 每日派生听/看/抽查三件套 |
| 状态 | md front matter `status: draft→final`；调度只消费 final。行政法(XZ) 为预留位，材料到位后启用 |
```

- [ ] **Step 2: 全量回归**

Run: `python -m pytest tests -v`
Expected: 全绿，0 failed

Run: `python scripts\validate_entries.py; python scripts\parse_md_to_json.py`
Expected: 校验 `-> 通过`（退出码 0）；转换两行 `OK:`

- [ ] **Step 3: 提交**

```powershell
git add README.md; if ($?) { git commit -m "docs: README 增补条目库管线章节" }
```

---

## 任务依赖图

```
Task1 ── Task2 ── Task3 ──┬── Task4 ── Task5 ── Task6 ── Task7 ── Task10
                          └──────────────────────┴── Task8（依赖4/5的校验器）
                                                 └── Task9（依赖7的tts与6的种子语义）
```

Task 8/9 相互独立，可与 Task 6/7 并行；Task 10 最后收尾。

## Self-Review 记录

1. **Spec 覆盖度**：第一节13字段→Task 2/3/8；第二节词表→Task 2/8；第三节示例行→Task 6；第四节存储布局/单元格语法/BOM→Task 1/2/3/6；第五节 JSON Schema→Task 3（新增 `md_sha256` 承接第八节「hash 计算」要求）；第六节 E/W 规则与报告→Task 4/5；第七节双视图→Task 9（三件套模板）+Task 7（tts_text 消费）；第八节五接口→Task 3/4+5/7/8/9；第九节自检项（路径惯例、读写分离、报告不入库、行政法预留）→Task 1/2/6/8。无缺口。
2. **占位符扫描**：全文无 TBD/TODO/「类似 Task N」；每个代码步骤均含完整可运行代码，每条命令均含期望输出。
3. **类型一致性**：`to_entry` 13 键、`load_document` 7 键、sidecar payload 7 键在各任务 Interfaces 与测试断言中逐一对应；`validate_files` 双注入参数（`material_names`/`known_materials`）在 Task 4/5 测试间用法一致；`levenshtein` 在 Task 4 定义、Task 5 W04 消费。
