# Plan：条目长度硬限制移除 · 存量截断修复 · TTS 法条先行

基于 Spec（2026-09-15-条目长度限制移除与截断修复-design）。

## Task 1：校验层去字数硬限制

**文件**：`backend/app/importer.py`（第 14-18、136-165 行）

- [x] 删除 `ANCHOR_MIN/ANCHOR_MAX`、`CONCLUSION_MAX`、`TTS_TEXT_MAX` 常量
- [x] `validate_entry` 移除 anchor 长度、conclusion 超长、tts 超长三条断言
- [x] conclusion 仅保留「以句号结尾」判断（待确认项 3 若改为放宽，则一并删除）
- [x] 其余校验（必填/ID/priority/cases/sources）保持不变

验收：`validate_entry` 对 75 字 anchor、120 字 conclusion、200 字 tts 返回 `[]`。

## Task 2：生成层改提示词与 fix 函数

**文件**：`backend/scripts/generate_entries.py`（`_build_prompt` 第 203-218 行、
`fix_anchor` 162 行、`fix_conclusion` 185 行、`fix_tts` 199 行）

- [x] `_build_prompt` 字数条款改为「建议范围 + 可越界」：point ≤15 / anchor 16-42 /
      conclusion ≤60 / tts_text ≤150，并注明不得为凑字数删减法条号
- [x] `fix_anchor` 去掉 36 字截断，改 `strip` + 超 42 字 WARN
- [x] `fix_conclusion` 去掉 59 字截断，仅补/规整结尾句号
- [x] `fix_tts` 去掉 150 字截断，仅 `strip`
- [x] TTS 法条先行规则写入提示词（第 4.3 节结构 + 禁 Markdown 标记）
- [x] 新增 `over_limit()`：超建议区间只打 WARN 并计入 `stats["over_limit"]`

验收：`fix_*` 对超长输入原样保留（仅补标点）。

## Task 3：测试同步

**文件**：`backend/tests/test_importer.py`（`test_validate_anchor_length` 第 75 行）、
`backend/tests/test_generate_entries.py`（第 164-190 行）

- [x] `test_validate_anchor_length` 改为「超长 anchor 不报错」，新增 conclusion/tts 超长放行用例
- [x] `test_fix_anchor_truncates_long` → 改为「超长 anchor 不被截断」
- [x] `test_fix_conclusion_truncates_and_adds_period` → 去掉长度断言，保留补句号断言
- [x] `test_fix_tts_truncates` → 改为「超长 tts 不被截断」
- [x] 新增 `tests/test_fix_truncated_entries.py`（5 个用例）

验收：`pytest` 全绿（170 passed）。

## Task 4：修复脚本

**文件**：`backend/scripts/fix_truncated_entries.py`（新增，≤300 行）

- [x] `scan_damaged(entries)`：返回 `[(index, entry, field, reason)]`，规则同 Spec 4.4
- [x] `build_prompt(batch)` / `parse_repairs(text)` / `repair_batch()`：批量请求 + 解析，未受损返回 null
- [x] `run()` 写回 `data/entries/<科目>.json`，输出逐条 diff
- [x] CLI：`--dry-run` / `--only=` / `--subject=` / `--id=` / `--batch=` / `--with-anchor` / `--limit`
- [x] 修复后自检：无 `第N。`、括号配对、无 Markdown、不短于原文、条号可核验
- [x] 额外增强：`statute_hints()` 从法条库取最相关条文候选，模型据此纠正错误条号

验收：`--dry-run` 输出受损清单（基线：conclusion 31、tts 3、anchor 463 候选）。

## Task 5：跑修复 → 落库 → 音频

- [x] `fix_truncated_entries.py --only=conclusion,tts`（31+3 处全部修复）
- [x] `fix_truncated_entries.py --only=anchor`（463 候选中 110 条确认为截断并补全）
- [x] 逐条 diff 复核（见执行结果）
- [x] `import_entries.py ../data/entries/*.json`（8 文件全部 OK）
- [x] `UPDATE entries SET status='final' WHERE status='draft'`（2426 条）
- [x] 删除变化条目的 `data/audio/MF-114|116|117.wav` → `synthesize_audio.py`（3/3 成功）
- [x] 复查：conclusion/tts 截断 0 条，DB final 2426 条

验收：复扫受损数 0；条目数不低于修复前；抽听音频与 tts_text 一致。

## Task 6：可选扩展（需用户确认后另行排期）

- [ ] 存量 1732 条有 `statutes` 条目的 TTS 法条先行批量改写（Spec 待确认项 1）
- [x] anchor 463 条候选修复（Spec 待确认项 4）
- [ ] 全库条号核对（启发式初筛出 180 条可疑引用，需 LLM/人工复核）

## Task 7：文档与提交

- [x] 更新 `AGENTS.md`（测试基线、踩坑记录）
- [x] 提交前跑 `pytest`（170 passed）
- [ ] **经用户确认后** commit（message：`feat(entry): relax length limits and repair truncated text`）

## 执行结果（2026-09-15）

| 项 | 结果 |
|---|---|
| 去硬限制 | `importer.py` 四个长度常量与三条断言已删；`fix_*` 不再截断，改只告警 |
| 提示词 | anchor 建议区间 16-42 字；四个字段均为「建议范围 + 可越界」；TTS 法条先行 |
| 修复 conclusion | 31 条全部补全（含条号纠正，如 `SG-067` 民诉法第275条 → 第279条） |
| 修复 tts_text | 3 条全部补全，`MF-114` 顺带改为法条先行 |
| 修复 anchor | 463 候选中 110 条确认为截断并补全，353 条判为完整未动 |
| 落库 | 8 个 JSON 导入 errors=0，2426 条 final |
| 音频 | 3 条变化条目重合成成功；anchor 变化不影响音频 |
| 测试 | `pytest` 170 passed |

**新发现（未处理，待确认）**：启发式核验发现全库 2077 条法条引用中 748 条无法在法条库解析
（公约/司法解释未收录，属正常），另有约 180 条条号与条文内容疑似不符（例：三国法涉外编
沿用 2021 年前条号，2023 年民诉法修改后应整体后移）。这是独立的数据质量任务。
