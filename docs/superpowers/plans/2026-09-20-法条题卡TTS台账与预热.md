# 法条题卡 TTS 台账、预热与全量生成 — 实施计划（2026-09-20）

输入 Spec：`docs/superpowers/specs/2026-09-20-法条题卡TTS台账与预热-design.md`

## Task 1 台账覆盖卡片（合成即建档）

文件：`backend/app/tts.py`、`backend/app/routers/audio.py`、`backend/tests/test_tts.py`

- [x] `tts.ensure_mp3(entry_id, text, *, dry_run=False, conn=None)`：本次真生成了新音频且
      `conn` 非空时调用 `record_asset()`（幂等 upsert）
- [x] `/api/audio/{entry_id}` 传入请求连接，按需听到的条目/卡片一并建档
- [x] 已有非空 wav 直接返回时**不**写库（保持快路径零写）
- [x] 测试：首次合成写台账；二次命中缓存不写；`dry_run` 不写

## Task 2 卡片质检档（不污染条目口径）

文件：`backend/app/tts_audit.py`、`backend/scripts/audit_tts.py`、
`backend/scripts/repair_tts.py`、`backend/tests/test_tts_audit.py`

- [x] `tts_audit.summarize(issues, *, repair_kinds=DEFAULT)`；默认行为与现在完全一致
- [x] `audit_tts.py --kind {entry,card,all}`（默认 `entry`，`v_entries` 隔离不变）
- [x] 卡片档：`missing` 计入「待生成」且不进 `repair_ids`；`empty/stale/suspect` 进
      `repair_ids`；`untracked` 单列
- [x] `repair_tts.py --kind {entry,card,all}`（默认 `entry`）；卡片档只修坏录音与
      `--ids`，绝不因 missing 触发批量合成
- [x] 测试：卡片 missing 归待生成、坏录音进 repair、条目档行为不回退

## Task 3 听学队列取卡（预热目标）

文件：`backend/app/service.py`、`backend/tests/test_service.py`

- [x] `service.listen_card_ids(conn, limit)`：复用 `_listen_pool` 排序，返回队列里的卡片 ID
- [x] 测试：顺序与听学池一致、`kind='entry'` 被过滤、limit 生效

## Task 4 合成脚本（预热 + 全量分片）

文件：`backend/scripts/synthesize_cards.py`（新增）、`backend/tests/test_synthesize_cards.py`

- [x] `--queue N`（默认 200）：预热听学队列头部缺音频的卡片
- [x] `--all --shard I/N`：候选集按 id 稳定排序后步长分片，供多 agent 并行
- [x] `--workers W`（默认 8）：线程池合成，主线程落盘 + 写台账（避开 SQLite 多线程写）
- [x] `--limit`、`--dry-run`、`--subject`（可选筛选）
- [x] 失败重试 2 次；失败不写 0 字节文件；已存在非空 wav 跳过（可中断续跑）
- [x] 测试：分片并集=全集且互不重叠、已存在音频跳过、dry-run 不落盘

## Task 5 验证与交付

- [x] `pytest tests` 全绿（443 passed）
- [x] `audit_tts.py`（条目档）回归：仍是 2426 条、0 待修复
- [x] 实跑 `synthesize_cards.py --queue 200` 并统计：卡片有音频 209、台账 209、坏录音 6 → 已修复
- [x] `audit_tts.py --kind card` 输出：待生成 11843 / 坏录音 0 / 未建档 0
- [x] 抽检 ≥3 张：时长与语速落正常区间（含修复前后对比）
- [x] `GET /api/audio/<卡片ID>` 命中缓存验证（0.20s）
- [ ] 输出全量生成的分片实测吞吐与预计耗时，交用户确认后再开多 agent 跑全量
      → 已给出（吞吐 154 字/秒；3 分片约 3.2 小时），**但磁盘可用 50 GB < 需 57 GB，
      需先在 §5 的 A/B/C 中选一条**：见 `docs/superpowers/research/2026-09-20-法条题卡TTS台账与预热报告.md`

## 提交纪律

- 全部改动先向用户汇报并取得确认后才 commit；message 用英文
  `type(scope): description`（如 `feat(tts): track and prewarm card audio`）。
