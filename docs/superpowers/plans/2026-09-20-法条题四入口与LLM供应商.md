# 法条驱动题四入口覆盖 + LLM 供应商切换 — 实施计划（2026-09-20）

Spec：`docs/superpowers/specs/2026-09-20-法条题四入口与LLM供应商-design.md`

## Task 1 LLM 供应商链（`app/ai.py`、`app/config.py`）

- [x] `config`：`OPENCODE_API_KEY/BASE_URL/MODEL`、`LLM_PROVIDER_ORDER`、`OPENCODE_MIN_MAX_TOKENS`、`LLM_CHAT_PROVIDER`
- [x] `ai.call_llm()` 走供应商链：opencode（带 session 头 + 推理预算，空串=失败）→ deepseek
- [x] `ai.provider_status()`：各供应商启用状态 + 最近命中的供应商（实测 `last_used=opencode`）
- [x] 测试：顺序、空响应回落、max_tokens 抬升、无 key 时返回 None；`conftest` 默认断网

## Task 2 法条卡数据层（`app/statute_cards.py`、`app/db.py`、`scripts/audit_quiz_entrypoints.py`）

- [x] `card_seen` 表（幂等建表，不碰 `reviews`/`quizzes` 结构）
- [x] `statute_cards.pool(conn, subjects=None, mode=..., limit=...)`：
      从 `quizzes(entry_id IS NULL, published)` 分组出法条卡，支持科目过滤与「未看过优先」轮转
- [x] `subjects` 语义改为**偏好**（硬过滤会让单日科目锁死覆盖）
- [x] `statute_cards.by_article(...)`：法条页挂题；`listen_text(card)`：听学朗读文本
- [x] 测试：池子只含法条驱动题、未看过优先、轮转可穷尽、按条查询、科目偏好不排除

## Task 3 自测混入（`app/quiz_service.py`）

- [x] `build_daily_quiz` 每 3 题留 1 题给法条驱动题（作答后写 `card_seen(mode='quiz')` 轮转）
- [x] 测试：题量 9 → 3 道法条题；答过的排最后；limit<3 行为不变

## Task 4 看背题卡（`app/routers/plans.py`、`app/service.py`、`FlashcardView.tsx`）

- [x] API：`/api/plans/today` 返回 `statute_cards`（每 3 张留 1 张，未看过优先）
- [x] 前端：`StatuteCardView` 题卡（先作答→看解析/条文）；下一张写 `card_seen(mode='read')`
- [x] Playwright 实测：第 11 张条目卡后出现「法条题卡 1/3」，作答回显解析与条文，可翻到 2/3

## Task 5 听学法条卡（`app/service.py`、`app/tts*.py`、`ListenView.tsx`）

- [x] `/api/listen` 返回 `statute_cards`；`/api/audio/card/<quiz_id>` 按需合成到 `data/audio/q<id>.wav`
      （实测 200 + audio/wav；成本随「实际听到的卡」增长）
- [x] 前端：**每听 3 段插 1 张**（排到队尾永远轮不到——队列会自动续批）
- [x] Playwright 实测：听完 3 段出现「听学法条题卡 1/3」，音频源指向卡片接口，看答案/条文正常

## Task 6 法条页挂题（`app/routers/statutes.py`、`StatuteReader.tsx`）

- [x] `GET /api/statute/questions?law=&no=&sub=` 返回该条题目（含条文原文）
- [x] 前端：条文展开后「练这一条（N 题）」折叠区 + 就地作答（复用 `/api/quiz/answer`）
- [x] 测试 + Playwright 实测：刑法第 140 条 2 题，作答回显正确答案/解析/条文

## Task 7 入口覆盖审计（`scripts/audit_quiz_entrypoints.py`）

- [x] 四个入口候选池 ⊇ 全部法条驱动题：**13160/13160，缺口 0**
- [x] 轮转穷尽由单测 `test_pool_repeated_rounds_can_exhaust_all` 保证
- [x] 报告落 `docs/superpowers/research/2026-09-20-法条题入口覆盖报告.md`

## Task 8 文档与提交

- [x] `pytest` 419 passed、`tsc`/`eslint` 无告警、Playwright 四入口实测
- [ ] AGENTS 3.x 流水线补「法条题四入口」段（等并发协作者的 AGENTS 改动落定）
- [x] 分批提交：①供应商链 ②法条卡数据层 ③自测 ④看背 ⑤听学 ⑥法条页 ⑦审计 ⑧判断题交互 ⑨文档
