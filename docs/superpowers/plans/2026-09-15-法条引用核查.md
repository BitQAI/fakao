# Plan：全库法条引用核查与高置信错配修正

基于 Spec（2026-09-15-法条引用核查-design）。

## Task 1：核查脚本（可复跑）

**文件**：`backend/scripts/audit_statute_citations.py`（新增，≤300 行）

- [ ] `parse_library(dir)`：解析 `data/法条库/*.md`，按「第 X 条」切分并**收全多段正文**，
      返回 `{文件名: {条号: 全文}}`；中文数字→整数需支持 十/百/千（`第二百条`=200）
- [ ] `ALIAS`：条目写法 → 法条库文件（民法典 / 公司法 / 刑诉法 / 民诉法 / 民诉解释 …）
- [ ] `parse_ref(s)`：解析 `刑法232条`、`民诉法第272条`、`公司法第23条第2款`
- [ ] `audit(root)`：输出 A 类（条号不存在）、B 类（库外法）、C 类（同一部法多种写法）、
      D 类（语义筛查：考点词与现引条文零重合 + 邻近唯一强候选）
- [ ] `--json` 输出机器可读结果，供 Task 2 引用

验收：A 类 0 条；D 类高置信 51 条（与人工确认集合一致）。

## Task 2：修正脚本

**文件**：`backend/scripts/fix_statute_citations.py`（新增，≤300 行）

- [ ] `FIXES`：`{条目ID: {"statutes": [(旧串, 新串|None)], "text": [(旧串, 新串)]}}`，
      只登记**读原文确认过**的条目；`None` 表示删除该引用
- [ ] `apply_entry(e)`：改 `statutes`（保序、去重）＋在 `conclusion/note/tts_text` 内做同串替换
- [ ] `self_check(e)`：三处条号一致、`statutes` 无重复、无空串
- [ ] `--dry-run` 默认 / `--apply` 写回；JSON 以 `ensure_ascii=False, indent=2` **无尾换行**写回，
      保证除改动行外零 diff
- [ ] 输出：逐条 old→new 清单 + 命中/未命中计数

验收：`--dry-run` 命中 51 条，`--apply` 后 `git diff --stat` 仅 8 个 entries JSON + 2 个脚本 +
2 份文档。

## Task 3：应用修正

- [ ] `.venv/bin/python scripts/fix_statute_citations.py --apply`
- [ ] `git diff` 抽查 5 条（SG-067 / MS-198 / MS-227 / SJ-557 / XZ-040）

## Task 4：验证

- [ ] 重跑 `audit_statute_citations.py`：A 类 0；高置信集合仅剩待确认项
- [ ] `.venv/bin/python -m pytest tests`（基线 170 passed）
- [ ] 脚本自检：51 条三处条号一致

## Task 5：同步数据库（本地）

- [ ] `scripts/import_entries.py ../data/entries/*.json`（errors=0）
- [ ] `UPDATE entries SET status='final' WHERE status='draft'`
- [ ] 复核 `select count(*) from entries where status='final'` = 2426

## Task 6：报告留痕

- [ ] `docs/superpowers/research/2026-09-15-法条引用核查报告.md`：A/B/C/D 四类结论、
      51 条修正明细、待确认项、低置信清单（95 条）

## 不做

- [ ] 不提交（commit 需用户确认）
- [ ] 不改生成链/校验链、不改结论规则表述、不补法条库
