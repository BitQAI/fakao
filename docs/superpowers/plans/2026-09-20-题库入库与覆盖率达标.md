# 题库进版本库与法条覆盖率达标 — 实施计划（2026-09-20）

Spec：`docs/superpowers/specs/2026-09-20-题库入库与覆盖率达标-design.md`

## Task 1 导出题库（`scripts/export_quiz_bank.py` + `backend/app/quiz_bank_io.py`）

- [x] `quiz_bank_io.export(conn, out_dir, origins=("bank","judge"), statuses=("published",))`
      → 按 origin+科目 写 `data/quiz_bank/<origin>-<subject>.json`
- [x] 稳定排序（entry_id, basis, stem）+ `count`/`exported_at` 头信息
- [x] CLI：`--out`、`--origins`、`--statuses`、`--dry-run`
- [x] 测试：导出文件结构、排序稳定、只含指定状态

## Task 2 导入题库（`scripts/import_quiz_bank.py`）

- [x] 读 JSON → `quiz_bank.save_question(..., status=published, replace=--replace)`
- [x] 返回并打印：写入 / 已存在跳过 / 条目缺失跳过 / 文件数
- [x] 测试：空库导入行数一致；重复导入行数不变（幂等）；条目缺失跳过不中断整批

## Task 3 覆盖率目标模式（`scripts/build_statute_quiz.py --target-ratio`）

- [x] `target_candidates(conn, ratio, laws)`：按每部法缺口反推候选（数字型优先）
- [x] CLI 接线 + `--dry-run` 打印还差多少条
- [x] 测试：缺口计算（含已覆盖条文、已达标法不再补、原配额模式不受影响）

## Task 4 跑到达标（≥80%）

- [x] 口径先修正为「只算 published 题」（草稿用户练不到；同时草稿会把下一轮缺口算成 0）
- [x] 第 1 轮 `--target-ratio 0.8`（4917 候选）：入库 4246 → 70.4%
- [x] 第 2 轮（1851 候选）：入库 1347 → 78.8%
- [x] 第 3 轮（504 候选）：入库 176 → 79.9%；通过率跌到 35%，
      91% 的失败是「正确项直接照抄条文」（程序型条文尤甚：生态环境法典 / 检察规则 / 刑诉解释）
- [x] 收紧出题提示词（正确项必须改写、不得出现连续 12 字与原文相同）后：
      第 4 轮（327 候选）入库 256 → 81.5%，第 5 轮 `--target-ratio 0.85`（794 候选）入库 727 → **86.1%**
- [x] 交付口径：**13738/15958 = 86.1%**（有题法 212/213），判断题核验 strict 不通过 0

## Task 5 交付

- [x] 导出最终题库到 `data/quiz_bank/`：**15987 题 / 16 个文件 / 14MB**（bank 14477 + judge 1510）
- [x] 往返验证（真实库副本）：清空 quizzes 后导入写入 15987、跳过 0；
      二次导入写入 0、跳过 15987（幂等）
- [x] 更新覆盖率报告 + Spec/Plan 勾选
- [x] `pytest` 402 passed、`tsc` 无报错、组卷/数判接口抽测正常
- [ ] 分批提交：①导出/导入骨架 ②按目标覆盖率出题 ③覆盖口径 ④题面改写提示词 ⑤题库数据 + 文档
