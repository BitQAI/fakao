# 法条驱动客观题库与遗留修复 — 实施计划（2026-09-20）

Spec：`docs/superpowers/specs/2026-09-20-法条驱动客观题库与遗留修复-design.md`

## Task 1 条文定位能力（`app/statutes.py`）

- [ ] `resolve_law_article(text) -> (law_key, no, sub) | None`：把「刑诉法91条」「中华人民共和国专利法第九条」解析成法条库定位
- [ ] `resolve_statute()` 改为复用该函数（行为不变）
- [ ] 测试：别名、条号写法、子条（之二）、解析失败

## Task 2 覆盖率度量（`app/statute_coverage.py` + `scripts/audit_quiz_coverage.py`）

- [ ] `coverage(conn)`：每部法 {条文数, 被条目引用, 被题目覆盖(entries/bank/judge), 覆盖率}
- [ ] 题目 `basis` 与条目 `statutes` 两种写法都能解析
- [ ] 报告：总量 + 覆盖率分布 + 缺口 Top30 + 分类（新增法条/司法解释/国际条约/配套法规）
- [ ] 输出 Markdown + JSON；测试覆盖解析与统计口径

## Task 3 法条驱动客观题（`scripts/build_statute_quiz.py`）

- [ ] 候选 A：条目引用过但无题覆盖的条文（按引用条目数排序）
- [ ] 候选 B：按 `--per-law` 配额补未覆盖条文（数字/要件型优先）
- [ ] 出题 → 校验（结构 + 正确项与条文原文相关）→ 入 `origin='bank'`、`entry_id=NULL`
- [ ] 参数：`--per-law`、`--limit`、`--workers`、`--only-cited`、`--only-missing`、`--publish`、`--dry-run`
- [ ] 抽检 + 发布；记录覆盖率前后变化

## Task 4 组卷可用（`app/quiz_service.py` + 测试）

- [ ] `custom_quiz`：条目题不足题量时用 `quiz_bank.pick` 补齐（含法条题）
- [ ] 测试：题量满时不改变结果；不足时补入法条题且不重复

## Task 5 遗留修复

- [ ] `build_judge_bank.py`：`--polarity true|false|auto` + `--include-covered`；补一轮「错」题至 45~55%
- [ ] 核验两级回源（条号 → 全文），报告区分 strict/loose
- [ ] 5 条无客观题条目：重试 + 放宽判据（结论 ∪ 场景），仍失败则列清单
- [ ] `fetch_statutes.py` 补《农村土地承包法》《种子法》等缺失法规（能补多少补多少）

## Task 6 验证与提交

- [x] `pytest` 全绿（354 passed）+ 前端 `tsc`/`eslint`
- [x] 覆盖率报告与抽检报告更新
- [x] 分批提交：①法条覆盖与法条题 ②遗留修复 ③文档/报告

## Task 7 追加修复（同日第二轮）

Spec：`…-design.md` 第 8 节。

- [x] `statutes.display_law_name()`：文件主名 → 规范法名，出题/入库/`--fix-basis` 三处统一口径
- [x] `build_statute_quiz.statute_basis()`：出题与入库共用同一依据写法（可反向解析）
- [x] `build_judge_bank.covered_basis()`：改为按「法名 + 条号」归一去重（原实现字符串直比，去重无效）
- [x] `build_judge_bank --fix-basis / --fix-subjects`：覆盖全部 `entry_id IS NULL` 行（判断题 + 法条题）
- [x] `quiz_service.custom_quiz`：法条题配额改确定性投放（`_pool_factor` 大池 + 分区排序）
- [x] `statutes.ordinal_blocks()`：「一、二、三、」式条文兜底解析（刑法修正案（十二）由 0 条 → 8 条）
- [x] 新一轮扩容：法条驱动客观题 4 批入库并发布（443 + 645 + 1045 + 567 = 2700 题尝试，
      入库 2700/3187），判断题侧再补 221 题；覆盖率 4260/15950（26.7%）→ **7006/15958（43.9%）**
      （数字见 `docs/superpowers/research/2026-09-20-法条覆盖与题库核验报告.md`，会随题库继续变动）
- [x] 已知缺口登记：刑诉法第292条解释（无条号结构）、多条约合一文件（条号重复）
- [ ] 提交：`refactor(statute): normalize statute display names in question basis` 等
