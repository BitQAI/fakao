# 题库扩容、数字判断题入口与 TTS 质检修复 — 实施计划（2026-09-20）

输入 Spec：`docs/superpowers/specs/2026-09-20-题库扩容与数字判断专项-design.md`

## Task 1 数据层：题库字段与 TTS 台账

文件：`backend/app/db.py`、`backend/tests/test_db.py`

- [x] 迁移前把 `quizzes` / `quiz_answers` 备份到 `data/backup/`（脚本内幂等）
- [x] `quizzes` 重建迁移：`qtype CHECK(choice,cloze,judge)` + 新增 `origin`(daily/bank/judge) / `basis` / `status`(draft/published) / `text_hash` / `subject` / `point`（entry_id 放宽为可空，供法条派生题）
- [x] 新增 `tts_assets` 表（entry_id, text_hash, speed, bytes, duration_sec, created_at）
- [x] 迁移幂等：重复 `db.connect()` 不重建、不丢行；旧库自动升级（实测 51 题 / 9 条记录无损）
- [x] 测试：新建库含新列；旧结构库与中间态库升级后行数不变、`origin` 回填 `daily`

## Task 2 题库服务层

文件：`backend/app/quiz_bank.py`（新增）、`backend/app/quiz_service.py`、`backend/tests/test_quiz_bank.py`

- [x] `quiz_bank.save_question(...)`（幂等写入，同 key 同指纹不重复计费）
- [x] `quiz_bank.pick(conn, *, origins, limit, subjects, points, qtypes)`：随机抽已发布题
- [x] `db.question_hash(stem, answer, options)`：题库幂等指纹
- [x] `quiz_service.build_daily_quiz`：优先取题库，缺题再走 LLM 现生成（实测今日自测 90% 出自题库）
- [x] `quiz_service.custom_quiz`：同源优先题库
- [x] `quiz_service.quiz_history` 增补 `basis` 下发
- [x] 测试：题库优先（断言不调 LLM）、缺题回退、未发布题不出现、历史含 basis

## Task 3 判断题（judge）题型

文件：`backend/app/quiz_bank.py`、`backend/app/routers/quizzes.py`、`backend/tests/test_judge_quiz.py`

- [x] `POST /api/quiz/judge`：入参 `subjects/points/limit`，返回 `qtype='judge'` 题组（默认 10 题）
- [x] `GET /api/quiz/bank/stats`：题库概览
- [x] `POST /api/quiz/answer` 支持 `judge`（对/错/正确/T/F 归一后比对）
- [x] 错题进 `quiz_answers`，自动进错题本与历史（复用现有链路）
- [x] 测试：对/错判定、非法答案、空题库兜底、limit 边界

## Task 4 TTS 文本与 key 降级

文件：`backend/app/tts_text.py`（新增）、`backend/app/tts.py`、`backend/app/config.py`、`backend/tests/test_tts_text.py`

- [x] `normalize(text)`：去 Markdown / 引号 / 符号→中文词 / 空白归一 / 句末补标点（实测后决定保留【标签】与《法名》）
- [x] `normalize` 幂等：`normalize(normalize(x)) == normalize(x)`
- [x] `tts.py`：主 key 全模型失败后回退 `DASHSCOPE_API_KEY_FALLBACK`
- [x] `.env` 增 `DASHSCOPE_API_KEY_FALLBACK`（用户提供的新 key，仅本地；实测主 key 正常，通道仅作保险）
- [x] 测试：符号替换、幂等、降级路径（mock）、台账 upsert

## Task 5 TTS 质检与修复

文件：`backend/scripts/audit_tts.py`（新增）、`backend/scripts/repair_tts.py`（新增）、`docs/superpowers/research/2026-09-20-TTS质检报告.md`

- [x] `app/tts_audit.py` + `scripts/audit_tts.py`：五类清单（缺失/空/陈旧/语速异常/未建档）+ 报告输出
- [x] `scripts/repair_tts.py --dry-run|--apply [--ids ...] [--backfill]`：先取新音频再覆盖，失败留旧文件
- [x] 修复已知 4 条截断（SJ-677 5.8s→19.0s、SG-339 3.7s→10.2s、SG-355 7.7s→21.3s、SJ-618 12.0s→18.6s）
- [x] 连同 13 条「文本改了但音频没重合成」与 6 条归一化改动，共重合成 23 条；复测异常 0

## Task 6 客观题题库生成

文件：`backend/scripts/build_quiz_bank.py`（新增）、`backend/tests/test_build_quiz_bank.py`

- [x] 按条目出题（anchor 场景 + conclusion 正确项 + 同 submodule 干扰项），写 `origin='bank'`
- [x] 参数：`--subject`、`--priority`、`--limit`、`--workers`、`--only-missing`、`--publish`、`--dry-run`
- [x] 校验：4 选项、答案合法、解析长度、**正确项与结论相关性**（3 种判据）
- [x] 全量 2421 题入库并发布（覆盖 99.8%），结构问题 0（见抽检报告）

## Task 7 数字判断题生成

文件：`backend/scripts/build_judge_bank.py`（新增）、`backend/tests/test_build_judge_bank.py`

- [x] 法条侧：抽取含数字表达的条文（期限/金额/人数/比例），按法名配额 + 数字密度取样；已出题法条自动跳过
- [x] 条目侧：从数字型结论中抽取核心考点（依据原文＝场景＋结论）
- [x] 硬校验：`app/number_terms.is_grounded`（「对」题不得有原文外数字，「错」题必须且只改一处）
- [x] **1052 题入库并发布**（法条侧 610 + 条目侧 442；另剔除 34 条纯生效日期题），`--fix-subjects` 修正科目口径

## Task 8 主观题题库扩容

文件：`backend/scripts/build_case_questions.py`、`build_composite_questions.py`、`build_essay_questions.py`

- [x] 案例题：按科目分 6 批扩容至 34 题（刑法/民法/民诉/行政法/商经知劳环各 +5 左右）
- [x] 综合大案例扩至 12 题
- [x] 论述题维持 21 题：现有上游专题已全部出题，新增需先补 `data/科目资料/理论法/` 素材（不臆造领导人讲话）
- [x] 全部走既有「采分点 + 维度」口径入库

## Task 9 前端入口与渲染

文件：`frontend/src/components/QuizView.tsx`、`JudgeQuizView.tsx`（新增）、`lib/types.ts`

- [x] 自测子页签加「数判」，默认 10 题，可选题量与科目（多选科目）
- [x] judge 渲染：对/错两键 → 即时解析 + 法条依据
- [x] 历史视图兼容 judge（显示用户答案/正确答案/依据）
- [x] `tsc --noEmit` / `eslint` 无告警

## Task 10 全量生成与抽检

- [x] 客观题库：2421/2426 条出题（3 轮 `--only-missing` 补跑）
- [x] 判断题：法条侧 388 + 条目侧 445
- [x] 抽检报告：`docs/superpowers/research/2026-09-20-题库抽检报告.md`

## Task 11 验证与文档

- [x] `pytest` 全绿（338 passed）
- [x] 前端 Playwright 实机验证「数判」闭环（进入→抽题→作答→解析→上下题→历史，控制台 0 错误）
- [x] 更新 `README.md`、`AGENTS.md`（题库/判断题/TTS 质检命令）

> 提交纪律：每个 Task 完成后先向用户确认再 commit（`type(scope): description`）。
