# 法条题卡「30 秒朗读文本」与全量音频生成 — 实施计划（2026-09-20）

输入 Spec：`docs/superpowers/specs/2026-09-20-法条题卡30秒朗读文本-design.md`

## Task 1 朗读文本改 30 秒版

文件：`backend/app/statute_cards.py`、`backend/tests/test_statute_cards.py`

- [ ] `LISTEN_MAX_CHARS = 110` 常量 + 句读优先裁剪函数
- [ ] `listen_text(card, max_chars=LISTEN_MAX_CHARS)`：依据 → 题干 → 答案 → 解析，
      预算 62%/38% 动态让额，输出归一后 ≤ max_chars
- [ ] 测试：长题干/长解析被裁到上限内、保留依据与答案、无「选项」「条文原文」、
      裁剪幂等（同输入同输出）
- [ ] 更新 `tests/test_card_entries.py` 中对旧格式（条文原文）的断言

## Task 2 同步卡片（旧音频与台账自动作废）

文件：数据同步，无代码

- [ ] `sync_card_entries.py --dry-run` 确认 12052 张全部变更
- [ ] 实跑同步，确认旧 wav / 台账行被清理，`tts_text` 全部为 30 秒版

## Task 3 30 秒硬校验

文件：`backend/app/tts_audit.py`、`backend/scripts/audit_tts.py`、
`backend/tests/test_tts_audit.py`

- [ ] `MAX_CARD_DURATION_SEC = 30`，卡片档把超时音频记为 `over_length` 并入 `repair_ids`
- [ ] 条目档口径不变（正式条目不受 30 秒限制）
- [ ] 测试：31 秒音频在卡片档被判 `over_length`，在条目档不判

## Task 4 抽样标定

- [ ] 抽 24 张卡片实合成，统计时长分布（目标 max ≤30 秒）
- [ ] 超限则下调 `LISTEN_MAX_CHARS` 重测；达标后锁定常量

## Task 5 全量生成（3 分片并行）

- [ ] `synthesize_cards.py --all --shard I/3 --workers 12`，3 个分片由 root + 2 个 agent 分担
- [ ] 各分片汇报进度；失败重跑（幂等跳过已完成）
- [ ] 完成后 `audit_tts.py --kind card` 全绿（待生成 0、坏录音 0、超时 0）
- [ ] 抽查 3 张：音频 ≤30 秒、台账 duration/字数匹配

## Task 6 交付

- [ ] 更新 `docs/superpowers/research/2026-09-20-法条题卡TTS台账与预热报告.md`
- [ ] 磁盘占用与总量统计
- [ ] 向用户汇报，取得确认后再 commit
