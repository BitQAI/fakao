# Plan：听背已听次数与自定义优先排序

基于 Spec `2026-09-01-听背已听次数与自定义优先排序` 的任务分解。

## Task 1：后端 review_stats 新增 listened_today

**文件**：`backend/app/review_stats.py`

**接口**：
```python
def attach_listened_today(conn, items: list[dict], day: str | None = None) -> list[dict]:
    """批量标记条目今天是否已听过（听背进度记忆，对标 attach_reviewed_today）。"""
```

**实现步骤**：
- [ ] 复制 `attach_reviewed_today` 逻辑，将 `mode='read'` 改为 `mode='listen'`，写入 `it.listened_today`
- [ ] 处理空 `items` 直接返回
- [ ] `day` 默认为 `date.today().isoformat()`
- [ ] 单测：构造含当日/非当日 listen 记录的 items，断言 `listened_today` 正确

**行数控制**：`review_stats.py` 现 117 行，新增函数约 15 行，总计 <150，远低于硬上限

---

## Task 2：后端 service 抽公共排序并统一自定义听背

**文件**：`backend/app/service.py`

**接口变更**：
```python
def _sort_listen_items(items: list[dict]) -> list[dict]:
    """未听优先 → 次数少优先 → 久未听优先 → 优先级 → 科目 → id"""

def _listen_pool(conn, exclude: set[str] | None = None) -> tuple[list[dict], int]:
    # 复用 _sort_listen_items

def custom_entries(..., listen_only: bool = False, ...) -> list[dict]:
    # listen_only=True 时复用 _sort_listen_items，末尾同时 attach_reviewed_today + attach_listened_today

def entry_by_id(conn, entry_id: str) -> dict | None:
    # 额外 attach_listened_today

def listen_queue(conn, limit: int = 100) -> dict:
    # 确保 items 已含 listened_today
```

**实现步骤**：
- [ ] 新增 `_sort_listen_items`：分组 `never/heard`，分别按 spec 排序 key 排序后合并
- [ ] 重构 `_listen_pool`：移除内联排序，调用 `_sort_listen_items`；`remaining` 查询不变；每条 `listened_today` 通过 `attach_listened_today` 或在排序前附加（注意 `last_ts` 已在 SQL 取出）
- [ ] 重构 `custom_entries(listen_only=True)` 分支：`attach_listen_counts` 后调用 `_sort_listen_items`，删除旧的 `items.sort(key=lambda e: listen_count...)` 内联逻辑；末尾 `return attach_listened_today(attach_reviewed_today(...))`
- [ ] `entry_by_id` 额外调用 `attach_listened_today`
- [ ] `listen_queue` 确保返回的 `slice_items` 已附加 `listened_today`
- [ ] 验证：自定义范围与默认池同数据集同序；同 `listen_count` 时 `last_ts` 早者在前

**行数控制**：`service.py` 现 456 行，新增函数约 25 行，重构后净增 <20，总计 <500

**依赖**：Task 1

---

## Task 3：前端类型补全

**文件**：`frontend/src/lib/types.ts`

**变更**：
```ts
export interface Entry {
  // ...
  listened_today?: boolean; // 新增，听背今日是否已听（对标 reviewed_today）
}
```

**步骤**：
- [ ] 在 `Entry` 接口 `reviewed_today` 下新增 `listened_today?: boolean`

---

## Task 4：前端 ListenView 显示对齐看背

**文件**：`frontend/src/components/ListenView.tsx`

**状态新增**：
```ts
const [listenedToday, setListenedToday] = useState<Set<string>>(new Set());
```

**UI 变更**：
- 进度行（对标 `FlashcardView.tsx:285`）：
  ```tsx
  <p className="muted">
    第 {idx+1} 段 / 队列 {items.length} · {entry.listen_count ? `本条已听 ${entry.listen_count}次` : "本条未听"} · 今日累计 {listenedToday.size} 条 · 累计已听 {heardTotal} · 剩余可听 {queue.remaining}
  </p>
  ```
- 标题徽标：
  ```tsx
  {entry.listen_count ? <span className="badge">已听 {entry.listen_count}次</span> : <span className="badge badge-ghost">未听</span>}
  {(entry.listened_today || listenedToday.has(entry.id)) && <span className="badge">今日已听</span>}
  {isMarked && <span className="badge">已标记</span>}
  ```

**逻辑变更**：
- [ ] 初始 `useEffect` 中 `GET /api/listen` 后：`setListenedToday(new Set(q.items.filter(i=>i.listened_today).map(i=>i.id)))`
- [ ] `applyCustom` 成功后：
  - 合并当日记录：`getJson("/api/reviews/history?mode=listen&limit=500")` 提取 `ts.slice(0,10)===today` 的 `entry_id`，与 `r.items.filter(i=>i.listened_today).map(id)` 合并入 `setListenedToday`
  - 失败回退仅用 `r.items` 的 `listened_today`
  - 同时 `setSeenIds`、`setRemaining` 保持现有
- [ ] `loadMore` 后：`setListenedToday(prev => { next=new Set(prev); r.items.forEach(it=>{if(it.listened_today) next.add(it.id)}); return next; })`
- [ ] `markExposed` 中：`dur<10` 不计分；`exposedRef` 去重；`if (!entry.listen_count)` 增 `heardTotal`；新增本地自增：
  ```ts
  setQueue(q => q ? { ...q, items: q.items.map(it => it.id===entry.id ? {...it, listen_count:(it.listen_count||0)+1, listened_today:true} : it) } : q);
  setListenedToday(prev => new Set(prev).add(entry.id));
  ```
  并 `POST /api/reviews` 保持不变

**行数控制**：`ListenView.tsx` 现 473 行，新增约 30 行，总计 ~503 行，需注意软上限 300 / 硬上限 500。**拆分策略**：若超限，将 `marksPanel` / `applyCustom` / `loadMore` 的纯逻辑抽至 `frontend/src/lib/listenStore.ts` 或 `frontend/src/hooks/useListenedToday.ts`；本次先评估，超限则拆。

---

## Task 5：测试与回归

**文件**：
- `backend/tests/test_service.py`（新增排序用例）
- `backend/tests/test_reviews.py`（新增 listened_today 用例）
- `backend/tests/test_quiz_continue.py`（自定义听学断言 `listened_today`）
- 前端手工 Playwright `localhost:3001`

**新增用例**：
- [ ] `test_attach_listened_today`：当日/昨日 listen 记录区分
- [ ] `test_custom_listen_sort_listen_count_and_last_ts`：0 次两条（优先级决定）+ 1 次两条（last_ts 早者在前）+ 3 次一条，断言顺序
- [ ] `test_listen_pool_and_custom_same_sort`：同数据集下 `_listen_pool` 与 `custom_entries(listen_only=True)` 顺序一致
- [ ] `test_listen_custom_includes_listened_today`

**回归**：
- [ ] `pytest -q` 全绿（预期 128+）
- [ ] 手工：
  1. 听背页刷新显示 `本条未听/本条已听 N次` 与 `今日累计`
  2. 播 ≥10s 暂停后徽标即时 `+1` 且出现 `今日已听`
  3. 自定义范围选 刑法 20 条，验证未听在前、1 次按久未听排序
  4. 分批 `下一组 20/50` 后 `remaining` 递减正确

## 执行顺序

Task 1 → Task 2（依赖1）→ Task 3 → Task 4（依赖3）→ Task 5

每个 Task 完成后不单独 commit；全部完成、用户确认后按 `type(scope): description` 提交。
