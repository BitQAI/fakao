# Plan：P0 口径修正 + P1 对标功能 + 全页面玻璃态 UI 重构

基于 Spec（2026-08-31-优化迭代-错题统计打卡组卷与玻璃态UI.md）的任务分解。执行顺序：后端数据/接口 → 前端功能 → UI 重构 → 回归与 e2e → 提交推送。

---

## Task 1：P0 后端修正（口径/覆盖树/导入）

**文件**：`backend/app/service.py`、`backend/app/coverage.py`、`backend/app/routers/import_.py`、`backend/tests/test_service.py`、`backend/tests/test_coverage.py`、`backend/tests/test_api.py`

- [ ] `today_stats.done` = 当日 read reviews + 当日 quiz_answers；percent 封顶 100；返回 `over_done` 标记
- [ ] `cov.entry_state` 扩展签名（last_result, review_count, quiz_wrong, has_record）；`coverage_payload` 查询补两字段；状态口径与调度统一
- [ ] Web 导入（`router/import_.py`）注入 `status='final'`
- [ ] 单测：完成率口径（看/听/测混合）、覆盖树 weak 含 quiz 错题、导入后 final 且可查询

## Task 2：P0 前端修正（听学暴露判定 + 文案）

**文件**：`frontend/src/components/ListenView.tsx`、`frontend/src/app/page.tsx`、`frontend/src/app/report/page.tsx`

- [ ] 跳过不再写 reviews；onPause/onEnded 仅 duration ≥10s 记 exposed
- [ ] 今日页/复盘页文案：`看背+自测 x/y（超额时显示超额完成）· 听学 z 分钟`

## Task 3：marks 表 + 标记 API + 前端接入

**文件**：`backend/app/db.py`、`backend/app/routers/marks.py`（新增）、`backend/app/service.py`、`backend/tests/test_marks.py`（新增）、`frontend/src/components/ListenView.tsx`、`frontend/src/lib/api.ts`、`frontend/src/lib/types.ts`、`frontend/src/lib/progressStore.ts`

- [ ] `db.py` 幂等新增 `marks` 表 + 索引
- [ ] `GET/POST /api/marks`、`DELETE /api/marks/{id}`、`DELETE /api/marks`（POST 幂等）
- [ ] 单测：幂等添加、删除、清空、条目不存在 404
- [ ] ListenView 标记改调 API；localStorage 旧标记合并上传后清除（逐条容错）

## Task 4：错题本（API + 学习页第 4 tab + 重练队列）

**文件**：`backend/app/routers/plans.py` 或新增 `backend/app/routers/wrongbook.py`、`backend/app/review_stats.py`、`backend/tests/test_reviews.py`、`frontend/src/app/study/page.tsx`、`frontend/src/components/WrongbookView.tsx`（新增）、`frontend/src/components/FlashcardView.tsx`、`frontend/src/lib/types.ts`

- [ ] `GET /api/wrongbook?limit=200`：聚合 quiz 错 + bad 评，按 last_wrong_ts 倒序
- [ ] `GET /api/wrongbook/queue?limit=30`：纯条目列表（复用 custom_entries 排序）
- [ ] 学习页 segments 扩为 4 个（看背/听学/自测/错题）；WrongbookView：列表 + 再看/再听 + 开始重练
- [ ] FlashcardView 支持 `?queue=wrong` 队列来源
- [ ] 单测：错题聚合口径、queue 返回、重练深链

## Task 5：学习统计 + 打卡/连胜（API + 报告页统计 segment + 今日页）

**文件**：`backend/app/stats.py`（新增，service 拆分承接）、`backend/app/routers/stats.py`（新增）、`backend/app/service.py`、`backend/tests/test_stats.py`（新增）、`frontend/src/app/report/page.tsx`、`frontend/src/components/StatsView.tsx`（新增）、`frontend/src/app/page.tsx`、`frontend/src/lib/types.ts`、`frontend/src/app/charts.css`

- [ ] `stats.py`：`overview(conn, days)`（日聚合/掌握度/streak）、`streak_info(conn, day)`
- [ ] `GET /api/stats/overview?days=90`；`/api/today` 增加 `streak`
- [ ] 报告页「统计」segment：连胜徽标 + 累计计数 + 掌握度分布 + 14 天折线 + 12 周热力图（SVG/div 自绘）
- [ ] 今日页：玻璃进度环（percent）+ 连续学习 N 天徽标
- [ ] 单测：日聚合补零、streak 计算（跨天/断档）、掌握度分布

## Task 6：智能组卷/限时模考（API + 自测页组卷 segment）

**文件**：`backend/app/routers/quizzes.py`、`backend/app/quiz_service.py`、`backend/tests/test_quiz_continue.py`、`frontend/src/components/QuizView.tsx`、`frontend/src/components/CustomRangePicker.tsx`（复用）、`frontend/src/lib/types.ts`

- [ ] `POST /api/quiz/custom`：custom_entries 过滤 + 当日题目缓存复用 + `{questions, timed, total}`
- [ ] QuizView segments 扩为「今日 / 组卷 / 历史」；组卷配置（范围/题量/限时）+ 限时倒计时（N×60s，超时自动结束）
- [ ] 单测：组卷过滤、缓存复用、限时参数透传

## Task 7：速记本（错题 tab 内抽问模式）

**文件**：`frontend/src/components/SpeedRecallView.tsx`（新增）、`frontend/src/components/WrongbookView.tsx`、`frontend/src/lib/types.ts`

- [ ] 错题 tab「我的标记」区：列表 + 重背/查看/取消/清空（调 Task 3 API）
- [ ] 速记模式：随机抽点 marks 条目 → 显示 point/anchor → 显示结论 → 自评（good/fuzzy/bad，写 reviews mode='read'）→ 下一张；顶部剩余/进度

## Task 8：玻璃态设计系统（令牌 + 基础组件 + 图标）

**文件**：`frontend/src/app/globals.css`（重写为令牌+基础）、`frontend/src/app/components.css`（新增）、`frontend/src/app/layout.tsx`、`frontend/src/components/icons.tsx`（新增）、`frontend/src/components/BottomNav.tsx`、`frontend/src/components/AiFab.tsx`

- [ ] 设计令牌：mesh 渐变背景（明/暗两套）、玻璃卡、渐变 primary、圆角/阴影/动效、tabular-nums、`@supports` 降级
- [ ] icons.tsx 内联 SVG 集；BottomNav 悬浮玻璃胶囊底栏 + SVG 图标；AiFab 玻璃悬浮钮
- [ ] 按钮/徽标/计数/segments/进度环基础类

## Task 9：全页面玻璃态改造

**文件**：`frontend/src/components/FlashcardView.tsx`、`ListenView.tsx`、`QuizView.tsx`、`WrongbookView.tsx`、`SpeedRecallView.tsx`、`StatsView.tsx`、`HistoryView.tsx`、`LeaderboardView.tsx`、`CoverageTree.tsx`、`CustomRangePicker.tsx`、`SourceViewer.tsx`、`AiChat.tsx`、`frontend/src/app/page.tsx`、`report/page.tsx`、`settings/page.tsx`、`frontend/src/app/charts.css`（新增）

- [ ] 闪卡玻璃翻转 + 光晕；自测玻璃选项；听学播放卡
- [ ] 报告五 segment（复盘/覆盖/历史/排行/统计）玻璃化 + 图表样式
- [ ] 今日/设置玻璃化；弹层 sheet 玻璃化；AI 聊天玻璃化
- [ ] 浅色/深色两套走查，无布局溢出；`npm run lint && npm run build` 通过

## Task 10：回归与 e2e

**文件**：`backend/tests/*`、`frontend/e2e/*.spec.ts`（新增，Playwright）、`frontend/playwright.config.ts`（新增）、`frontend/package.json`

- [ ] 后端 `pytest` 全量通过（147 + 新增）
- [ ] Playwright 主链路：今日（进度环/连胜）→ 看背评分 → 听学播放+标记 → 自测 → 组卷限时 → 错题重练 → 速记 → 报告统计 → 截图走查（浅/深色）
- [ ] 行数自查：所有业务文件 < 硬上限（service.py 拆分后 < 500；组件 < 500）
- [ ] 提交（**先向用户确认**，message 用英文 `type(scope): description`）

## Task 11：推送远端

- [ ] `git push origin feat/fakao-tool`（推送前向用户确认）

---

## 依赖顺序

Task 1→2（P0）可先合；Task 3→4→7 串行（marks → 错题 → 速记）；Task 5、6 可与 3/4 并行（不同文件）；Task 8→9 覆盖全部前端，最后做样式收尾；Task 10 回归，Task 11 推送。
