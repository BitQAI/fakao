# 案例库模糊检索与文书化阅读 — 实施计划（2026-09-22）

> 依据：`docs/superpowers/specs/2026-09-22-案例库模糊检索与文书化阅读-design.md`
> 附带：`docs/superpowers/specs/2026-09-22-条目数据格式化元数据规范.md` 的配套干跑校验脚本

## 执行前提（实测于 2026-09-22）

- `cases` 表 7172 行，4 库齐全；`app/cases.get_case` 具备懒装载能力，直接复用。
- `/api/cases/search` **不可用**：会被 `cases_quiz` 的 `/api/cases/{qid}`（`qid:int` 但 Starlette
  compile_path 用 str 转换器）先匹配 → 422。故统一走单数 `/api/case/*`。
- 前端 :3001、后端 :8090；e2e 用 `/Users/huajun/.hermes/venv/bin/python frontend/e2e/smoke_test.py` 的 Playwright。
- 基线：`pytest tests` 当前全绿（170 passed / 已增至 200+）。

## 任务清单

### Task 1 — 案例索引层 `backend/app/case_index.py`（新增）

- [ ] `sections(text, source) -> (list[{label,text}], dropped)`：三种分节策略（`【X】` / 裸行小标题 / 前置编号要旨）+ 碎段合并 + 连续重复段删除
- [ ] `search(conn, q, source=None, limit=30)`：多词 AND、字段加权、片段截取与排序
- [ ] `stats(conn)`：总数 + 各来源条数
- [ ] 纯标准库实现，复用 `app.cases.CASE_SOURCES` 与 `cases.get_case`

### Task 2 — 路由 `backend/app/routers/case_library.py`（新增）+ 注册

- [ ] `GET /api/case/search`、`GET /api/case/detail`、`GET /api/case/stats`
- [ ] `main.py` 在 `cases_quiz.router` **之前**注册（双保险，避免路径竞争）
- [ ] 非法 `source` → 404；未命中 → 404；空 `q` → 空列表

### Task 3 — 后端测试（新增）

- [ ] `backend/tests/test_case_index.py`：分节（4 来源）、重复段删除、AND 检索、标题优先、来源过滤、limit、空 q
- [ ] `backend/tests/test_api_case_library.py`：三接口 + `/api/cases/{qid}` 回归
- [ ] `pytest tests` 全绿

### Task 4 — 前端案例库页面

- [ ] `frontend/src/lib/caseLibTypes.ts`：`CaseHit` / `CaseDetail` / `CaseSection` / `CaseStats`
- [ ] `frontend/src/components/CaseLibraryList.tsx`：命中列表 + 高亮
- [ ] `frontend/src/components/CaseDocument.tsx`：文书化阅读（标题区 + 分节）
- [ ] `frontend/src/app/cases/page.tsx`：搜索 / 筛选 / 列表 ↔ 详情（URL 参数驱动，可分享链接）
- [ ] `frontend/src/components/BottomNav.tsx`：新增「案例」tab（`/cases`）
- [ ] `frontend/src/app/components.css`：新增 `.caselib-*` 样式块（不动既有 `.case-*`）

### Task 5 — 条目规范配套干跑校验（对应元数据规范 §7 第 1 步）

- [ ] `backend/scripts/validate_entries.py`：复用 `importer` 的校验逻辑，**不写库**，输出 E/W 与条数汇总，退出码 0/1
- [ ] `backend/tests/test_validate_entries.py`：干净文件 0、坏文件 1（ID 非法 / 缺字段）

### Task 6 — 验证

- [ ] `cd backend && .venv/bin/python -m pytest tests`
- [ ] 起后端 `:8090`、前端 `:3001`，Playwright 走查 `/cases`：搜索 → 结果 → 详情分节 → 无 console 报错 → 截图
- [ ] 回填 `docs/superpowers/research/2026-09-22-案例库检索与文书化阅读报告.md`（实测数据与截图路径）

## 提交安排（需用户确认后才 commit）

建议两个提交：

1. `docs(entries): add entry data formatting and metadata spec`（元数据规范 + 干跑校验脚本）
2. `feat(cases): add fuzzy case search and document-style reader`（后端 + 前端 + 测试）

## 风险与回退

- 风险：分节解析对个别案例正文标记不全会退化为「无标题正文」——可接受（不丢文本，仅少一个小标题）。
- 风险：`LIKE` 全表扫描在 7172 行下约数十毫秒；若后续案例量级翻十倍再考虑 FTS5（本期不做）。
- 回退：前端仅新增页面与一个 tab，删除 `/cases` 页面 + nav 项即完全回退；后端新增文件独立，不涉及表结构。
