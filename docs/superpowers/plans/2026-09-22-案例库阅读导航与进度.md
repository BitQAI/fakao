# 案例库阅读导航与看阅进度 — 实施计划（2026-09-22）

> 依据：`docs/superpowers/specs/2026-09-22-案例库阅读导航与进度-design.md`
> 状态：已完成（2026-09-22），未提交（等用户确认）

## Task 1 — 阅读记录表 `backend/app/db.py`

- [x] `SCHEMA_SQL` 增加 `case_views` 表 + `idx_case_views_last` 索引（`IF NOT EXISTS`，连接时执行，无需迁移函数）

## Task 2 — 记录读写 `backend/app/case_views.py`（新增）

- [x] `VIEWED_FILTERS = ("", "no", "yes")`
- [x] `mark_viewed(conn, source, loc) -> dict`：upsert（`first_viewed_at` 保留、`last_viewed_at` 刷新、`views+1`），返回含 `first_time`
- [x] `viewed_keys(conn) -> set[(source, loc)]`、`is_viewed(conn, source, loc) -> bool`
- [x] `history(conn, limit=30) -> dict`：JOIN `cases` 取标题等信息，`last_viewed_at` 倒序；`total` = 记录数
- [x] `clear(conn) -> int`

## Task 3 — 检索/相邻篇 `backend/app/case_index.py`

- [x] `SORTS` 增加 `unviewed`
- [x] 抽出 `_ordered(conn, q, source, field, crime, year, sort, viewed_filter)`：复用筛选 + 排序，返回全量命中行并写入 `viewed`
- [x] `search(..., viewed_filter=None)`：`items` 带 `viewed`，`filters` 回显 `viewed`；`unviewed` 排序 = 未看过组在前 + 组内原序
- [x] `neighbors(conn, lib, loc, ...)`：当前篇按「未看过」定位算 `next`，
  `prev` 走 `case_views.previous_view` 阅读轨迹，返回 `{prev, next, index, total}`
- [x] `case_views.previous_view(conn, source, loc)`：轨迹上一篇（含同毫秒并列的确定回退）
- [x] `viewed=no` 筛选在相邻篇里忽略（打点后本篇不再匹配，否则前后篇全空）
- [x] `_matches` 增加 `viewed` 过滤分支

## Task 4 — 路由 `backend/app/routers/case_library.py`

- [x] `/api/case/search` 增加 `viewed` 参数与校验（未知值 → 400）
- [x] `GET /api/case/neighbors`（校验 sort/field/year/viewed，`lib` 走 `_check_source`）
- [x] `POST /api/case/view`（body: source/loc；案例不存在 → 404）
- [x] `GET /api/case/history`、`DELETE /api/case/history`

## Task 5 — 后端测试

- [x] `tests/test_case_views.py`（新增）：upsert 幂等、`views` 累计、历史倒序与 JOIN 标题、清空
- [x] `tests/test_api_case_library.py` 扩展：`viewed` 字段 / `sort=unviewed` / `viewed=no|yes` /
  `neighbors` 首末边界 / `/view` 404 / `history` + DELETE / 非法 `viewed` 400
- [x] `tests/test_case_index.py` 扩展：`neighbors` 与 `search` 顺序一致
- [x] `.venv/bin/python -m pytest tests` 全绿

## Task 6 — 前端类型与 URL 上下文

- [x] `lib/caseLibTypes.ts`：`CaseHit.viewed`、`CaseSort` += `unviewed`、`CaseFilters.viewed`、
  `CaseNeighborItem` / `CaseNeighbors` / `CaseHistoryItem` / `CaseHistoryResult`
- [x] `lib/caseNav.ts`（新增）：`filtersFromParams` / `readerContextFromParams` / `searchPath` /
  `readerPath` / `neighborPath`

## Task 7 — 前端阅读视图

- [x] `components/CaseReader.tsx`（新增）：吸顶导航（返回 / 上一篇 / 下一篇 / 第 N/M 篇 / 首次阅读徽标）
  + `←`/`→` 快捷键 + 进入打点（先 neighbors 后 view），正文复用 `CaseDocument`
- [x] `app/cases/page.tsx`：筛选状态从 URL 初始化；点开案例带上全上下文；返回保留上下文

## Task 8 — 前端列表与历史

- [x] `components/CaseLibraryList.tsx`：`已看` 徽标
- [x] `components/CaseFilterBar.tsx`：排序新增「未看过优先」并作为默认；「阅读状态」chips（全部/未看过/已看过）
- [x] `components/CaseHistoryPanel.tsx`（新增）：可折叠「最近看过（N）」+ 清空历史
- [x] `app/components.css`：`.caselib-viewed` / `.casereader-nav` / `.casehist-*`

## Task 9 — 验证

- [x] `pytest tests`、`npx tsc --noEmit`、`npm run lint`
- [x] 起后端 :8090 + 前端 :3001，浏览器实测：未看过优先、徽标、吸顶下一篇、`→` 键、返回保留筛选、历史回跳
- [x] 实测结论回填 Spec §8 或研究文档

## 提交安排（需用户确认）

建议单提交：`feat(cases): add reader navigation and viewed tracking`
