# 核心考点覆盖扩容与指导性案例挂接 — Plan（2026-08-29）

## T1 指导性案例入库与挂接

- [x] T1.1 解析精选汇编 30 个指导性案例，与 278 条按 id 合并（全部已存在，正文/关键词增强）；index.csv 按 (来源库, 定位符) 合并不丢行（含回归测试）。
- [x] T1.2 `load_cases.py` 装载完成（7172 条）。
- [x] T1.3 `match_cases.py --append-guiding` + 领域门槛（刑事→刑法/刑诉，民事/知产→民法/民诉/商经知+三国法涉外，行政/国赔→行政法）；测试 8 个。
- [x] T1.4 全量挂接：286 条条目、495 处指导性案例引用（原 12 处）；240 条超限条目裁剪为 ≤3（优先保留指导性案例）。

## T2 定向补缺

- [x] T2.1 `generate()` 支持 `blocks` 参数；新增 `load_blocks` / `uncovered_blocks` / `gap_blocks`（剔除复习/口诀/速记等元信息节）+ `--gaps` 模式；测试 6 个。
- [x] T2.2 以 `--gaps` 内建实现，无需独立脚本。
- [x] T2.3 首轮 7 科补缺新增 92 条（刑法2/民法18/刑诉5/民诉3/商经知16/理论法7/三国法41），导入并恢复 final。
- [x] T2.4 充值后重跑三轮收敛：66 → 7；商经知表格块改用 `--batch 2` 解决（避免多表格合批超 token 截断），本轮 +57；全库 1005 块覆盖 998，未覆盖 7（刑法1/商经知2/理论法4，多为 loc 匹配损耗）。

## T3 行政法

- [x] T3.1 编写 3 份科目资料（117 块，392 行），依据法条库核实（处罚简易 200/3000 元、当场收缴 100 元、许可 20+10 日、强制 30+30 日、复议 60+30 日、诉讼 6 个月/15 日、国赔 2 个月/3 个月/20 倍等）。
- [x] T3.2 代码接入：`SUBJECT_PREFIX` 加 `行政法: XZ`；scheduler `SUBJECT_ORDER` 与 generator 续批循环加入行政法。
- [x] T3.3 生成 159 条（0 失败、4 丢弃），优先级分布 高频101/易错42/新增2/普通14。
- [x] T3.4 导入并 final；行政法 117 块覆盖 103，未覆盖 14（部分为 loc 匹配损耗，如国家赔偿范围）。

## T4 音频与收尾

- [x] T4.1 累计 382 条新条目音频全部合成成功（2304/2304，无空文件）。
- [x] T4.2 全量 pytest 125 通过；最终覆盖 998/1005（99.3%）。改动未提交，等确认。

## 提交节奏

每个 Task 完成后提交一次（commit 前需用户确认），Commit message 遵循 `type(scope): description`。
