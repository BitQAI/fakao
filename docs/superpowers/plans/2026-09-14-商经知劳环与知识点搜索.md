# Plan：知识点搜索 +「商经知劳环」扩容

基于 Spec（2026-09-14-商经知劳环与知识点搜索-design）。

## Task 1：共享搜索逻辑与搜索框组件（前端）

**文件**：`frontend/src/lib/pointSearch.ts`（新增）、`frontend/src/components/KeywordSearch.tsx`（新增）、`frontend/src/app/components.css`

- [x] `searchTree(tree, keyword)`：返回 `{ points, subjects, subs, total, active }`；命中考点名或父级（子模块/科目）名即命中
- [x] 关键词归一化：去首尾空白、全角空格、大小写不敏感
- [x] `KeywordSearch`：受控输入 + 清空按钮 + 命中数展示
- [x] `.search-bar` 样式（与玻璃态卡片一致）

## Task 2：覆盖页搜索

**文件**：`frontend/src/components/CoverageTree.tsx`

- [x] 顶部接入 `KeywordSearch`
- [x] 命中时只渲染命中科目/子模块/考点，并强制展开；清空恢复原折叠行为
- [x] 无命中时提示「无匹配考点」

## Task 3：自定义范围搜索

**文件**：`frontend/src/components/CustomRangePicker.tsx`

- [x] 按科目模式：按关键词过滤科目列表
- [x] 按知识点模式：命中分支自动展开、只显示命中考点
- [x] 「全选命中结果」按钮（仅搜索态出现），「取消全选命中结果」
- [x] 筛选不改变已勾选集合；确认时仍提交完整勾选集合

## Task 4：科目改名 商经知 → 商经知劳环

**文件**：`backend/app/scheduler.py`、`backend/app/generator.py`、`backend/scripts/generate_entries.py`、`backend/scripts/match_cases.py`、`backend/tests/test_generator.py`、`backend/tests/test_generate_entries.py`、`backend/tests/test_match_cases.py`、`data/科目资料/`、`data/entries/`、`AGENTS.md`

- [x] 代码/测试/文档里的科目名与顺序统一改名（ID 前缀仍为 `SJ`）
- [x] `git mv data/科目资料/商经知 data/科目资料/商经知劳环`（资料文件名保持不变，`sources.ref` 不失效）
- [x] `git mv data/entries/商经知.json data/entries/商经知劳环.json` 并更新 553 条 `subject` 与 `source_md`
- [x] DB：`UPDATE entries SET subject='商经知劳环' WHERE subject='商经知'`
- [x] `pytest` 全绿（163 passed）

## Task 5：新增劳动法 / 环境保护法资料

**文件**：`data/科目资料/商经知劳环/劳动法-高频考点.md`（新增）、`data/科目资料/商经知劳环/环境保护法-高频考点.md`（新增）

- [x] 劳动法：服务期与违约金、竞业限制、集体合同、非全日制用工、劳务派遣、工伤认定与待遇、年休假、工时加班、社会保险、失业保险金（25 块）
- [x] 环保法：基本原则与责任主体、环评、排污许可、总量控制、监测与信息公开、按日连续处罚、查封扣押、行政拘留、生态红线/补偿、环境侵权（民法典 1229-1235）、公益诉讼、水污染防治（25 块）
- [x] 每块含条文号，保证 LLM 摘录能逐字命中原文

## Task 6：生成条目 → 导入 → 音频

- [x] `generate_entries.py --subject 商经知劳环 --gaps --batch 3`（52 块，新增 123 条，fail 0）
- [x] 校验 `errors=0` 后 `import_entries.py ../data/entries/商经知劳环.json`
- [x] `UPDATE entries SET status='final' WHERE status='draft'`
- [x] `synthesize_audio.py` 仅补新条目（123/123 成功）
- [x] `/api/coverage` 复查新子模块与条数（商经知劳环 675 条，含环境保护法 56 条）

## Task 8：附带修复自测题与考点脱钩（执行中新发现）

**文件**：`backend/app/ai.py`、`backend/tests/test_ai.py`

- [x] 复现：`SJ-593`（年休假天数）生成出刑法题
- [x] 修复：`_quiz_brief(entry)` 拼入科目/子模块/考点/场景/结论/易错提示/法条
- [x] 回归测试 + 清理 2 条错误缓存题；重新出题验证与考点一致
- [x] 逐条复核新增条目法条引用，修正 5 条、删除 1 条无法条依据条目

## Task 7：自验与提交

- [x] 后端 `pytest` 全绿（163 passed）；条目来源引用 0 失效
- [x] 前端 `tsc --noEmit` / `eslint` / `npm run build` 通过
- [x] Playwright（本机 Chrome）实测：覆盖页搜「环境」命中 60 考点、自定义范围搜「年休假」命中 3 考点 + 全选命中 + 勾选保持 + 开始学习加载新科目卡片
- [ ] 汇总改动，等用户确认后 commit（message：`feat(subject): rename 商经知 to 商经知劳环 and add point search`）

## 执行顺序

Task 1 → 2 → 3（前端搜索，可独立自验）→ Task 4（改名）→ 5 → 6（数据流水线）→ 7（自验 + 提交确认）。
